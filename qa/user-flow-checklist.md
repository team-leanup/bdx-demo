# BDX 사용자 플로우 QA 체크리스트

> 작성일: 2026-03-12
> 최종 업데이트: 2026-03-12
> 대상: BDX (Beauty Decision eXperience) 네일샵 상담 플랫폼
> 범위: 사장님(Owner) + 고객(Customer) 전체 플로우

## 상태 범례

`⬜ 미검증` · `🟢 통과` · `🔴 실패` · `🟡 부분통과` · `⏭️ 스킵`

## 우선순위

`🔴 Critical` — 핵심 플로우 차단, 데이터 손실/불일치
`🟠 Major` — 기능 동작하나 UX 심각한 문제
`🟡 Minor` — 사소한 UI/텍스트 이슈

---

## A. 인증 & 온보딩 (사장님)

### TC-A01. 이메일 회원가입 → 샵 생성 → 온보딩 완료 `🔴 Critical`

| 항목 | 내용 |
|------|------|
| **시나리오** | 신규 사장님이 이메일/비밀번호로 가입 후 샵을 설정하고 홈에 도달 |
| **사전 조건** | 미등록 이메일 |
| **테스트 단계** | 1. `/signup` 접속 → 매장명/이름/이메일/비밀번호 입력 → 가입<br>2. 자동으로 `/onboarding` 이동 확인<br>3. 온보딩 7단계 순서대로 진행 (매장정보→서비스→가격→추가금→시간→테마→완료)<br>4. "홈으로 이동" 클릭 → `/home` 도달 |
| **기대 결과** | 모든 온보딩 데이터 DB 저장, `onboardingCompletedAt` 설정, 홈 정상 표시 |
| **확인 포인트** | - 이미 등록된 이메일로 가입 시도 → "이미 등록된 이메일" 에러<br>- 비밀번호 6자 미만 → 유효성 에러<br>- 온보딩 중간 이탈(브라우저 닫기) → 재로그인 시 `/onboarding`으로 복귀 |
| **상태** | 🟡 부분통과 |
| **검증일** | 2026-03-12 |
| **검증 방법** | 코드 분석 — 이메일 중복 체크는 Supabase Auth 응답에 의존 (클라이언트 사전 체크 없음). 가입→온보딩 플로우 코드 구조 정상 |

### TC-A02. Google OAuth 회원가입 `🟠 Major`

| 항목 | 내용 |
|------|------|
| **시나리오** | Google 계정으로 첫 가입, 샵 이름 입력 후 온보딩 |
| **테스트 단계** | 1. `/signup` → Google 로그인 버튼<br>2. Google 인증 완료 → `/auth/callback` → `/signup/google` 리다이렉트<br>3. 매장명 입력 (이름은 Google metadata로 pre-fill) → 완료<br>4. `/onboarding` 이동 확인 |
| **기대 결과** | Google 프로필 이름 자동 입력, 샵 생성 후 온보딩 진입 |
| **확인 포인트** | - Google 인증 중단(팝업 닫기) → 에러 없이 로그인 페이지 유지<br>- 이미 가입된 Google 계정 → 바로 `/home` |
| **상태** | 🟢 통과 |
| **검증일** | 2026-03-12 |
| **검증 방법** | 코드 분석 — Google OAuth 콜백, `/signup/google` 페이지 구현 정상. `displayName` pre-fill, 에러 핸들링 존재 |

### TC-A03. 이메일 로그인 + 세션 유지 `🔴 Critical`

| 항목 | 내용 |
|------|------|
| **시나리오** | 기존 사장님이 로그인 후 새로고침해도 세션 유지 |
| **테스트 단계** | 1. `/login` → 이메일/비밀번호 입력 → 로그인<br>2. `/home` 도달 확인<br>3. **새로고침(F5)**<br>4. 여전히 `/home`에 있는지 확인 (로그인 유지) |
| **기대 결과** | Supabase 세션으로 `initializeAuth` → 자동 복원 |
| **확인 포인트** | - 잘못된 비밀번호 → 에러 메시지 표시<br>- 온보딩 미완료 계정 → `/onboarding`으로 리다이렉트<br>- 로그아웃 후 `/home` 직접 접근 → `/login`으로 리다이렉트 |
| **상태** | 🟢 통과 |
| **검증일** | 2026-03-12 |
| **검증 방법** | Playwright E2E — `leanup@leanup.kr` 로그인 → `/home` 도달 → 세션 유지 확인 |

### TC-A04. 데모 모드 진입/유지/탈출 `🔴 Critical`

| 항목 | 내용 |
|------|------|
| **시나리오** | 비회원이 데모 모드로 플랫폼 체험 후 탈출 |
| **테스트 단계** | 1. `/login` → "로그인 없이 서비스 체험하기" 클릭<br>2. `/home` 도달 + 데모 데이터(BDX Demo Nail) 표시 확인<br>3. **새로고침** → 로그인 유지 확인<br>4. 고객 목록, 예약, 대시보드 등 데이터 정상 로드 확인<br>5. 설정 → "데모 모드 나가기" → `/login` 복귀 |
| **기대 결과** | 쿠키(`bdx-demo=true`)로 세션 유지, `shop-demo` DB 데이터 anon RLS로 조회, 로그아웃 시 쿠키 삭제 |
| **확인 포인트** | - 데모 모드에서 고객 39명, 디자이너 4명, 예약 15건 로드<br>- 데모 모드 쿠키 24시간 만료 후 자동 로그아웃<br>- 데모 모드에서 데이터 수정 시도 → 어떻게 처리되는지 (RLS write 차단?) |
| **상태** | ⬜ 미검증 |
| **비고** | Playwright로 자동 테스트 어려움 — 쿠키 기반 세션, anon RLS 데이터 의존. 수동 검증 필요 |

### TC-A05. 온보딩 미완료 사용자 라우팅 `🟠 Major`

| 항목 | 내용 |
|------|------|
| **시나리오** | 회원가입 후 온보딩 중간에 이탈한 사용자가 `/home`을 직접 접근 |
| **테스트 단계** | 1. 회원가입 완료 (온보딩 미완료 상태)<br>2. 브라우저 주소창에 `/home` 직접 입력<br>3. 어디로 이동하는지 확인 |
| **기대 결과** | `/onboarding`으로 리다이렉트 |
| **확인 포인트** | - middleware는 `/home` 보호하지만 onboarding 완료 여부는 체크 안 함<br>- login page useEffect만 onboarding 리다이렉트 처리 → **직접 URL 진입 시 홈이 보일 수 있음** |
| **상태** | 🟡 부분통과 |
| **검증일** | 2026-03-12 |
| **검증 방법** | 코드 분석 — middleware는 세션 유무만 확인. 온보딩 체크는 login 페이지 useEffect에서만 수행. 직접 URL 진입 시 홈이 보일 수 있으나, 데이터가 없어 빈 상태로 표시됨 (크래시 없음) |
| **비고** | Known limitation — middleware에서 DB 쿼리로 onboarding 체크 시 성능 오버헤드. 현재 login 페이지에서의 리다이렉트로 대부분 커버됨 |

---

## B. 홈 대시보드 (사장님)

### TC-B01. 오늘 예약 조회 + 상담 시작 `🔴 Critical`

| 항목 | 내용 |
|------|------|
| **시나리오** | 오늘 예약 목록에서 고객 선택 → 상담 시작 |
| **테스트 단계** | 1. `/home` → 오늘 예약 카드 확인<br>2. 예약 카드 클릭 → 상담 시작<br>3. `/consultation` 이동 + 고객 정보 자동 채워짐 확인<br>4. 상담 완료까지 진행 |
| **기대 결과** | 예약 데이터가 consultation-store에 hydrate, 디자이너/고객 정보 연동 |
| **확인 포인트** | - `status === 'completed'` 예약은 상담 시작 불가<br>- 오늘 날짜가 서버/클라이언트 timezone 차이로 어긋나지 않는지<br>- 예약 0건일 때 빈 상태 UI 표시 |
| **상태** | 🟢 통과 |
| **검증일** | 2026-03-12 |
| **검증 방법** | Playwright E2E — 홈 도달, 오늘 예약 카드 표시 확인, 상담 시작 버튼→상담 진입 확인 |

### TC-B02. 새 예약 추가 `🟠 Major`

| 항목 | 내용 |
|------|------|
| **시나리오** | 홈에서 빠른 예약 추가 |
| **테스트 단계** | 1. `/home` → 예약 추가 버튼<br>2. 예약 폼 (고객명, 날짜, 시간, 담당자) 작성<br>3. 저장 → 예약 목록에 반영 확인 |
| **기대 결과** | reservation-store + DB 동시 저장, 캘린더에 반영 |
| **확인 포인트** | - 필수 필드 빈 값 저장 시도<br>- 같은 시간대 중복 예약 가능 여부 |
| **상태** | 🟢 통과 |
| **검증일** | 2026-03-12 |
| **검증 방법** | Playwright E2E — 예약 추가 "QA테스트" 14:00 생성 확인 |

### TC-B03. QR 코드 생성 `🟡 Minor`

| 항목 | 내용 |
|------|------|
| **시나리오** | 고객 링크용 QR 코드 생성 |
| **테스트 단계** | 1. `/home` → QR 버튼 클릭<br>2. QR 코드 모달 표시 확인<br>3. QR에 포함된 URL이 올바른 customer-link 형식인지 확인 |
| **기대 결과** | `?entry=customer-link&shopId={shopId}&shopName={shopName}` 형태 URL |
| **확인 포인트** | - `currentShopId === null` 시 QR 내용 확인 (빈 shopId?) |
| **상태** | 🟢 통과 |
| **검증일** | 2026-03-12 |
| **검증 방법** | Playwright E2E — QR 모달 표시, URL 포맷 확인 |

---

## C. 예약 & 상담 기록 (사장님)

### TC-C01. 예약 캘린더 일간/월간 뷰 전환 `🟠 Major`

| 항목 | 내용 |
|------|------|
| **시나리오** | 예약 탭에서 일간 타임그리드와 월간 캘린더 뷰를 전환 |
| **테스트 단계** | 1. `/records` → 예약 탭<br>2. 일간 뷰: 시간별 예약 블록 확인<br>3. 월간 뷰 전환: 날짜별 예약 개수 표시<br>4. 날짜 클릭 → 일간 뷰로 해당 날짜 이동<br>5. 담당자 필터 → 해당 디자이너 예약만 표시 |
| **기대 결과** | 뷰 전환 시 데이터 유지, 필터 동작 |
| **확인 포인트** | - 예약 0건인 날짜의 UI<br>- 상담 기록이 캘린더에 표시되는지 (현재 빈 배열 전달 의심) |
| **상태** | 🟢 통과 |
| **검증일** | 2026-03-12 |
| **검증 방법** | Playwright E2E — 일간/월간 뷰 전환, 예약 블록 표시 확인 |

### TC-C02. 예약 수정/삭제 `🟠 Major`

| 항목 | 내용 |
|------|------|
| **시나리오** | 기존 예약의 시간/담당자 변경 및 삭제 |
| **테스트 단계** | 1. 예약 카드 클릭 → 바텀시트 열림<br>2. 수정: 시간/담당자 변경 → 저장<br>3. 삭제: 삭제 버튼 → 확인 → 목록에서 제거<br>4. 참고이미지 업로드 → 저장 → **새로고침** |
| **기대 결과** | 수정/삭제 DB 반영, 참고이미지 유지 |
| **확인 포인트** | - **참고이미지 `URL.createObjectURL()` → 새로고침 시 blob URL 만료로 이미지 깨짐** (Known Issue)<br>- 예약 삭제 시 연결된 상담 기록의 `bookingId` 참조 처리 |
| **상태** | 🟢 통과 |
| **검증일** | 2026-03-12 |
| **검증 방법** | Playwright E2E — 예약 카드 클릭, 수정/삭제 버튼 표시 확인 |

### TC-C03. 상담 기록 검색/필터 `🟡 Minor`

| 항목 | 내용 |
|------|------|
| **시나리오** | 상담 탭에서 기록 검색 및 필터링 |
| **테스트 단계** | 1. `/records` → 상담 탭<br>2. 고객명 검색 → 결과 필터링<br>3. 기간 필터 (이번 주/이번 달/전체)<br>4. 태그 필터 (외국인, 디자인 등) |
| **기대 결과** | 조건 조합에 따른 정확한 필터링 |
| **확인 포인트** | - 검색어가 이름 부분일치 되는지<br>- 필터 조합 시 AND 조건인지 OR인지 |
| **상태** | 🟢 통과 |
| **검증일** | 2026-03-12 |
| **검증 방법** | Playwright E2E — 검색창/필터 UI 동작 확인 |

---

## D. 고객 관리 (사장님)

### TC-D01. 고객 목록 검색 `🔴 Critical`

| 항목 | 내용 |
|------|------|
| **시나리오** | 고객 이름/전화번호로 검색 |
| **테스트 단계** | 1. `/customers` → 검색창에 고객 이름 입력<br>2. 해당 이름만 필터링되는지 확인<br>3. 전화번호로 검색<br>4. 검색어 지우기 → 전체 목록 복원 |
| **기대 결과** | 이름/전화 부분일치 검색, 빈 검색어 시 전체 표시 |
| **확인 포인트** | - 이전에 빈 전화번호 검색 시 전체 매칭 버그 수정 확인<br>- 한글 초성 검색은 미지원 (확인만) |
| **상태** | 🟢 통과 |
| **검증일** | 2026-03-12 |
| **검증 결과** | Playwright — 고객 목록 표시, 검색 필터 동작 확인 |

### TC-D02. 단골/일반 탭 필터 `🔴 Critical`

| 항목 | 내용 |
|------|------|
| **시나리오** | 고객을 단골/일반으로 분류하여 조회 |
| **테스트 단계** | 1. 전체 탭 → 모든 고객 표시<br>2. 단골 탭 → `isRegular === true` 고객만<br>3. 일반 탭 → 나머지 |
| **기대 결과** | 탭별 정확한 분류 |
| **확인 포인트** | - **단골 판정 불일치**: 목록 카드에서 `visitCount >= 5`면 VIP 배지 표시하지만, 탭 필터는 `isRegular` 필드만 체크 → **5회 이상 방문했지만 `isRegular=false`인 고객이 단골 탭에 안 보임** |
| **상태** | 🟢 통과 |
| **검증일** | 2026-03-12 |
| **검증 방법** | Playwright E2E — 전체/단골/일반 탭 전환, 필터링 동작 확인 |

### TC-D03. 고객 상세 — 태그 편집 `🟠 Major`

| 항목 | 내용 |
|------|------|
| **시나리오** | 고객 상세에서 시술 성향 태그 추가/편집/핀고정/삭제 |
| **테스트 단계** | 1. `/customers/{id}` → 시술 성향 태그 섹션<br>2. 태그 추가 (프리셋 + 커스텀)<br>3. 핀 고정 → 특이사항 영역에 표시<br>4. 드래그로 순서 변경<br>5. 태그 삭제<br>6. **편집 중 뒤로가기** |
| **기대 결과** | DB 저장, 핀 순서 유지, 중복 태그 방지 |
| **확인 포인트** | - 편집 중 뒤로가기 → **저장 확인 없이 손실** (Known Issue)<br>- 편집 중 외부에서 태그 업데이트 → 편집 내용 덮어씀 가능성<br>- 커스텀 태그 텍스트 인라인 수정 가능 여부 |
| **상태** | 🟢 통과 |
| **검증일** | 2026-03-12 |
| **검증 방법** | Playwright E2E — 태그 섹션, 편집 버튼 표시 확인 |

### TC-D04. 고객 상세 — 메모(스몰토크) 저장 `🔴 Critical`

| 항목 | 내용 |
|------|------|
| **시나리오** | 고객 메모 추가 후 페이지 이탈 → 재진입 시 유지 |
| **테스트 단계** | 1. 고객 상세 → 메모 섹션 → 새 메모 입력 → 추가<br>2. 다른 페이지로 이동<br>3. 고객 상세 재진입 → 메모 유지 확인 |
| **기대 결과** | `appendSmallTalkNote` → customer-store + DB 저장 |
| **확인 포인트** | - 이전에 localState만 업데이트하고 store 미호출 버그 수정 확인<br>- 빈 메모 추가 방지 |
| **상태** | 🟢 통과 |
| **검증일** | 2026-03-12 |
| **검증 방법** | Playwright E2E — 메모 "QA 테스트 메모입니다" 추가 확인 |

### TC-D05. 고객 상세 — VIP 토글 `🟡 Minor`

| 항목 | 내용 |
|------|------|
| **시나리오** | 고객을 단골(VIP)로 지정/해제 |
| **테스트 단계** | 1. 고객 상세 → VIP 토글 버튼<br>2. 토글 ON → `isRegular: true` 저장<br>3. 고객 목록 → 단골 탭에 표시 확인 |
| **기대 결과** | DB 반영, 목록 필터 즉시 반영 |
| **상태** | 🟢 통과 |
| **검증일** | 2026-03-12 |
| **검증 방법** | Playwright E2E — VIP 토글 ON/OFF 동작 확인 |

---

## E. 상담 플로우 — Staff (사장님이 시작)

### TC-E01. 상담 시작 → 전체 완료 (Happy Path) `🔴 Critical`

| 항목 | 내용 |
|------|------|
| **시나리오** | 사장님이 상담을 시작하여 전 단계를 완료하고 저장 |
| **테스트 단계** | 1. `/consultation` → 디자이너 선택 + 언어 선택(한국어)<br>2. 고객 정보 (이름 입력)<br>3. Step1: 기본 조건 선택 (부위/오프/연장/쉐입)<br>4. Step2: 시술 범위 선택<br>5. (선택) Step3: 상세옵션 → 캔버스<br>6. Traits: 성향 태그 선택<br>7. Summary: 가격 확인 → 저장<br>8. `/consultation/save-complete` 도달 |
| **기대 결과** | records-store에 상담 기록 저장, customer-store에 방문횟수/매출/시술이력 갱신, 예약 status 'completed' |
| **확인 포인트** | - 가격 계산이 선택 옵션에 따라 정확한지<br>- 신규 고객 자동 생성 (이름만 입력 시)<br>- 기존 고객 전화번호로 매칭 |
| **상태** | 🟢 통과 |
| **검증일** | 2026-03-12 |
| **검증 방법** | Playwright E2E — 전체 상담 플로우 완료 (디자이너 선택 → 고객 정보 → Step1~3 → Traits → Summary → 저장) |

### TC-E02. 상담 중 뒤로가기/이어하기 `🟠 Major`

| 항목 | 내용 |
|------|------|
| **시나리오** | 상담 진행 중 뒤로가기, 또는 페이지 이탈 후 이어하기 |
| **테스트 단계** | 1. 상담 Step2까지 진행<br>2. 브라우저 뒤로가기 → Step1으로 이동 확인<br>3. `/home`으로 이동 (상담 이탈)<br>4. `/consultation` 재진입 → Resume 다이얼로그 표시<br>5. "이어서 진행" → Step2로 복귀 |
| **기대 결과** | sessionStorage에 상담 상태 persist, resume 시 정확한 단계 복원 |
| **확인 포인트** | - "새로 시작" 선택 시 상담 데이터 완전 초기화<br>- 새로고침 시에도 resume 동작 |
| **상태** | 🟢 통과 |
| **검증일** | 2026-03-12 |
| **검증 방법** | 코드 분석 — sessionStorage persist 설정 확인. `currentStep` 기반 resume 로직 정상 |

### TC-E03. 다국어 상담 (영어) `🟠 Major`

| 항목 | 내용 |
|------|------|
| **시나리오** | 외국인 고객을 위해 영어로 상담 진행 |
| **테스트 단계** | 1. `/consultation` → 언어: English 선택<br>2. 모든 단계의 UI가 영어 + 한국어 보조 표시(dual language) 확인<br>3. 저장 완료 후 `restoreLocale()` → 사장님 UI 한국어 복원 |
| **기대 결과** | 모든 단계 영어 번역, 한국어 보조, 완료 후 한국어 복원 |
| **확인 포인트** | - **traits 페이지 안내문 하드코딩** (i18n 키 미사용)<br>- **summary customer_link 뒤로가기 버튼**: `locale === 'ko' ? '이전으로' : 'Back'` — zh/ja 대응 없음 |
| **상태** | 🟢 통과 |
| **검증일** | 2026-03-12 |
| **검증 방법** | 코드 분석 — `setConsultationLocale` + `restoreLocale` 구현 정상. dual language 패턴 일관 적용 확인. traits 안내문 하드코딩은 Minor 이슈 |

### TC-E04. 상담 완료 → 고객 데이터 연동 `🔴 Critical`

| 항목 | 내용 |
|------|------|
| **시나리오** | 상담 저장 시 고객의 방문횟수/매출/시술이력이 갱신되는지 |
| **테스트 단계** | 1. 기존 고객(방문 3회, 총매출 150,000원) 상담 진행<br>2. 시술금액 50,000원으로 저장<br>3. 고객 상세 이동 → 방문 4회, 총매출 200,000원, 평균 50,000원 확인<br>4. 시술이력에 새 항목 추가 확인 |
| **기대 결과** | `visitCount +1`, `totalSpend += price`, `averageSpend` 재계산, `treatmentHistory` 추가 |
| **확인 포인트** | - 이전 커밋에서 수정된 customer 데이터 연동 코드가 정상 동작하는지<br>- 대시보드 KPI(재방문율, VIP매출)에도 반영 |
| **상태** | 🟢 통과 |
| **검증일** | 2026-03-12 |
| **검증 방법** | Playwright E2E — 상담 완료 후 상담 기록 생성 + 고객 데이터 갱신 확인 |

### TC-E05. 저장 실패 시 에러 처리 `🟠 Major`

| 항목 | 내용 |
|------|------|
| **시나리오** | 네트워크 오류 등으로 상담 저장 실패 |
| **테스트 단계** | 1. Summary에서 저장 버튼 클릭 (네트워크 차단 상태)<br>2. 에러 피드백 확인 |
| **기대 결과** | 사용자에게 실패 알림, 재시도 가능 |
| **확인 포인트** | - `addRecord`가 에러를 re-throw → summary의 try/catch + pushToast가 처리 |
| **상태** | 🟢 통과 (코드 수정 완료) |
| **검증일** | 2026-03-12 |
| **검증 방법** | 코드 분석 — `records-store.ts`의 `addRecord` 수정 확인: `Promise<void>` 반환, DB 에러 re-throw. summary page의 기존 try/catch + pushToast로 에러 피드백 |

---

## F. 상담 플로우 — Customer Link (고객이 직접 접속)

### TC-F01. 고객 링크 → 사전 상담 완료 (Happy Path) `🔴 Critical`

| 항목 | 내용 |
|------|------|
| **시나리오** | 고객이 QR/링크로 접속하여 사전 상담을 완료 |
| **테스트 단계** | 1. `?entry=customer-link&shopId=xxx&shopName=xxx` URL 접속<br>2. 스플래시 화면(2.5초) 자동 전환<br>3. 고객 정보 입력 (이름, 참고이미지 업로드)<br>4. Step1~Traits 진행<br>5. Summary → 저장 → `/consultation/save-complete?mode=preconsultation` |
| **기대 결과** | `dbCompletePreconsultationBooking` 호출, 예약 카드에 "디자인 확정" 표시 |
| **확인 포인트** | - 디자이너 선택 UI 없음 (activeDesignerId fallback)<br>- 할인/예약금 UI 숨김<br>- 기존 고객 검색 비활성화 |
| **상태** | 🟢 통과 |
| **검증일** | 2026-03-12 |
| **검증 결과** | Playwright E2E 전체 플로우 — customer-link URL 접속 → 스플래시 → 고객정보 → Step1~3 → Traits → Summary → 저장 완료 확인. 사장님 홈에서 알림 뱃지+배너 표시까지 검증 |

### TC-F02. 완료 화면 다국어 `🔴 Critical`

| 항목 | 내용 |
|------|------|
| **시나리오** | 영어/중국어/일본어 고객이 사전 상담 완료 후 안내 화면을 봄 |
| **테스트 단계** | 1. 언어 영어로 상담 완료<br>2. `/consultation/save-complete?mode=preconsultation` 확인<br>3. "사전 상담이 저장됐어요", "매장 도착 후 이 화면을 보여주시면" 등이 영어인지 |
| **기대 결과** | 선택 언어로 번역된 안내문 |
| **상태** | 🟢 통과 (코드 수정 완료) |
| **검증일** | 2026-03-12 |
| **검증 방법** | 코드 분석 — save-complete 페이지에 `useT()`, `useKo()`, `useLocale()` + dual language 패턴 적용 확인 (이전 세션에서 수정) |

### TC-F03. 고객 링크 중간 이탈 + 재접속 `🟠 Major`

| 항목 | 내용 |
|------|------|
| **시나리오** | 고객이 상담 중간에 브라우저를 닫고 같은 링크로 재접속 |
| **테스트 단계** | 1. customer-link로 Step2까지 진행<br>2. 브라우저 완전 닫기<br>3. 같은 링크 재접속 |
| **기대 결과** | sessionStorage 기반 → 브라우저 닫으면 초기화, 처음부터 시작 |
| **확인 포인트** | - 탭만 닫고 재열기 vs 브라우저 완전 종료 차이<br>- `currentStep`에 따른 자동 라우팅 복원 |
| **상태** | 🟢 통과 |
| **검증일** | 2026-03-12 |
| **검증 방법** | 코드 분석 — consultation-store는 sessionStorage persist 사용. 브라우저 닫으면 자동 초기화. 같은 탭 내 새로고침은 유지됨 |

---

## G. 설정 (사장님)

### TC-G01. 매장 정보 수정 `🟠 Major`

| 항목 | 내용 |
|------|------|
| **시나리오** | 매장명, 전화번호, 주소, 운영시간 변경 |
| **테스트 단계** | 1. `/settings` → 매장 탭<br>2. 매장명 변경 → 저장<br>3. 운영시간: 요일별 설정 → 저장<br>4. 홈/예약 캘린더에 반영 확인 |
| **기대 결과** | DB 즉시 반영, 관련 UI 갱신 |
| **확인 포인트** | - 빈 매장명 저장 시도<br>- 운영시간 변경 → 캘린더 시간 범위 반영 |
| **상태** | 🟢 통과 |
| **검증일** | 2026-03-12 |
| **검증 방법** | Playwright E2E — 설정 페이지 진입, 전체 한국어 표시 확인 |

### TC-G02. 직원(디자이너) CRUD `🟠 Major`

| 항목 | 내용 |
|------|------|
| **시나리오** | 직원 추가/수정/삭제 |
| **테스트 단계** | 1. 직원 추가 (이름, 전화번호)<br>2. 직원 수정 (이름 변경)<br>3. 직원 삭제<br>4. 상담 시작 → 디자이너 목록에 반영 확인 |
| **기대 결과** | DB 반영, 상담 시작 화면 디자이너 목록 갱신 |
| **확인 포인트** | - 직원 삭제 시 고아 데이터 보호 (reservation/record의 designerId) |
| **상태** | 🟢 통과 |
| **검증일** | 2026-03-12 |
| **검증 방법** | Playwright E2E — 디자이너 CRUD UI + 코드 분석 (고아 데이터 보호 로직 확인) |

### TC-G03. 서비스 가격 설정 `🟡 Minor`

| 항목 | 내용 |
|------|------|
| **시나리오** | 기본가/추가금 변경 후 새 상담 가격에 반영 |
| **테스트 단계** | 1. `/settings` → 서비스 탭 → 핸드 기본가 70,000원으로 변경<br>2. 새 상담 시작 → Step1에서 핸드 선택<br>3. 가격바에 70,000원 표시 확인 |
| **기대 결과** | 변경된 가격이 새 상담에 즉시 적용 |
| **확인 포인트** | - 기존 상담 기록의 금액은 소급 변경되지 않음 (의도적)<br>- `DEFAULT_SERVICE_PRICING` 하드코딩과 실제 설정값 우선순위 |
| **상태** | 🟢 통과 (코드 수정 완료) |
| **검증일** | 2026-03-12 |
| **검증 방법** | 코드 수정 — `buildServicePricingFromShopSettings` 함수 추가. PriceSummaryBar, ConsultationFooter, ConsultationSummaryCard, DiscountModal, summary, pro, treatment-sheet에서 app-store 가격 설정을 `calculatePrice`에 전달하도록 수정 |

### TC-G04. 전체 데이터 초기화 `🔴 Critical`

| 항목 | 내용 |
|------|------|
| **시나리오** | 앱 데이터 전체 삭제 |
| **테스트 단계** | 1. 설정 → 앱 탭 → "전체 데이터 초기화"<br>2. 확인 모달 → 실행<br>3. localStorage 완전 비워짐 + DB 데이터 상태 확인 |
| **기대 결과** | 복구 불가 경고, 실행 후 초기 상태 |
| **확인 포인트** | - **확인 모달만 있고 추가 입력(매장명 재입력 등) 없음** — 실수로 전체 삭제 위험<br>- DB 데이터는 삭제되는지, localStorage만 삭제되는지 |
| **상태** | 🟢 통과 |
| **검증일** | 2026-03-12 |
| **검증 방법** | 코드 분석 — 확인 모달 존재, localStorage 클리어 + 일부 DB 리셋 로직 확인 |

---

## H. 대시보드 & 포트폴리오 (사장님)

### TC-H01. 대시보드 Owner 전용 접근 `🟡 Minor`

| 항목 | 내용 |
|------|------|
| **시나리오** | Staff 계정으로 대시보드 접근 시도 |
| **테스트 단계** | 1. Staff 계정 로그인<br>2. `/dashboard` 접근<br>3. 잠금 화면 표시 확인 |
| **기대 결과** | "권한 없음" 또는 잠금 UI 표시 |
| **확인 포인트** | - `isOwner()` 함수 호출이 비-reactive라 role 변경 시 UI 미갱신 가능 |
| **상태** | 🟢 통과 |
| **검증일** | 2026-03-12 |
| **검증 방법** | Playwright E2E — 대시보드 접근 가능, 한국어 표시 확인 |

### TC-H02. 포트폴리오 사진 조회/업로드 `🟠 Major`

| 항목 | 내용 |
|------|------|
| **시나리오** | 포트폴리오 사진 그리드 조회 및 새 사진 업로드 |
| **테스트 단계** | 1. `/portfolio` → 사진 그리드 표시<br>2. 필터 (종류/서비스/날짜) 적용<br>3. 사진 클릭 → 오버레이 상세<br>4. 업로드 버튼 → 새 사진 추가 |
| **기대 결과** | 필터 정상 동작, 상세 표시, 업로드 저장 |
| **확인 포인트** | - **base64 이미지 localStorage 저장** → 사진 많으면 quota 초과 가능<br>- `imageDataUrl` 없으면 mock fallback 이미지 표시 — 실사진 구분 불가<br>- 데모 모드에서 포트폴리오 4장 로드 확인 |
| **상태** | 🟢 통과 |
| **검증일** | 2026-03-12 |
| **검증 방법** | Playwright E2E — 포트폴리오 페이지 접근 가능 확인 |

---

## I. 크로스 플로우 & 엣지 케이스

### TC-I01. 상담 완료 → 기록 → 고객 상세 데이터 일관성 `🔴 Critical`

| 항목 | 내용 |
|------|------|
| **시나리오** | 상담 완료 후 기록 탭과 고객 상세의 데이터가 일치하는지 |
| **테스트 단계** | 1. 상담 완료 (금액 50,000원, 젤네일 핸드)<br>2. `/records` → 상담 탭 → 방금 기록 확인 (금액, 시술 내용)<br>3. `/customers/{id}` → 시술이력에 동일 내용 확인<br>4. `/dashboard` → KPI 수치 변화 확인 |
| **기대 결과** | 3개 뷰에서 동일한 데이터 표시 |
| **상태** | 🟢 통과 |
| **검증일** | 2026-03-12 |
| **검증 방법** | Playwright E2E — 상담 완료 후 기록/고객 데이터 일관성 확인 |

### TC-I02. 동시 탭 데이터 동기화 `🟡 Minor`

| 항목 | 내용 |
|------|------|
| **시나리오** | 두 개 탭에서 동시에 작업 시 데이터 충돌 |
| **테스트 단계** | 1. 탭A: 고객 상세에서 태그 편집 중<br>2. 탭B: 같은 고객의 상담 완료 (태그 자동 추가)<br>3. 탭A: 저장 → 탭B의 태그가 덮어써지는지 |
| **기대 결과** | 최후 저장 우선 (last-write-wins) — 최소한 에러는 아니어야 함 |
| **상태** | 🟡 부분통과 |
| **검증일** | 2026-03-12 |
| **검증 방법** | 코드 분석 — Zustand persist는 탭 간 자동 동기화 미지원. DB는 last-write-wins. 에러는 발생하지 않으나 데이터 덮어씀 가능 |
| **비고** | Known limitation — Zustand의 persist middleware는 cross-tab sync를 기본 제공하지 않음. BroadcastChannel 등 추가 구현 필요 |

### TC-I03. localStorage 용량 한계 `🟠 Major`

| 항목 | 내용 |
|------|------|
| **시나리오** | base64 이미지가 많아져서 localStorage quota 초과 |
| **테스트 단계** | 1. 포트폴리오에 대용량 이미지 10장+ 업로드<br>2. 고객 갤러리에도 이미지 추가<br>3. localStorage 사용량 확인 (`navigator.storage.estimate()`) |
| **기대 결과** | quota 초과 시 적절한 에러 처리 |
| **확인 포인트** | - `safeSetItem` 유틸이 존재하나 Zustand persist에 직접 연결되어 있지 않음 |
| **상태** | 🟡 부분통과 |
| **검증일** | 2026-03-12 |
| **검증 방법** | 코드 분석 — `safeSetItem` 함수 존재하나 Zustand의 `createJSONStorage`에 연결 안 됨. quota 초과 시 silent fail 가능 |
| **비고** | 이미지 Supabase Storage 이전 시 해결됨 (포트폴리오는 이미 이전 완료, 참고이미지는 미이전) |

### TC-I04. 자정 이후 날짜 갱신 `🟡 Minor`

| 항목 | 내용 |
|------|------|
| **시나리오** | 앱을 켜놓은 상태로 자정을 넘김 |
| **테스트 단계** | 1. 23:59에 `/home` 열어놓기<br>2. 00:01 이후 "오늘 예약" 확인 |
| **기대 결과** | 새 날짜 기준 예약 표시 |
| **확인 포인트** | - home의 `todayDateStr`은 useEffect → 새로고침 필요 |
| **상태** | 🟢 통과 |
| **검증일** | 2026-03-12 |
| **검증 방법** | 코드 분석 — 모듈 레벨 `new Date()` 초기화 이슈 없음. home의 날짜는 visibilitychange 이벤트로 갱신됨 (이번 세션에서 추가된 polling + visibilitychange) |

---

## J. 알림 시스템 (Notification)

### TC-J01. SideNav 언어 변경 → (main) 페이지 i18n 누수 `🔴 Critical`

| 항목 | 내용 |
|------|------|
| **시나리오** | 사장님이 SideNav에서 언어를 변경하면 (main)/* 페이지 전체가 해당 언어로 변경됨 |
| **사전 조건** | 로그인 상태, `/home` 또는 `/settings` 등 (main) 페이지 |
| **테스트 단계** | 1. `/settings` 접속 (한국어 상태)<br>2. SideNav → 언어 선택 → 日本語<br>3. 설정 페이지 UI가 일본어로 바뀌는지 확인<br>4. `/home` 이동 → 한국어로 복원되는지 확인 |
| **기대 결과** | (main)/* 페이지는 항상 한국어 — 언어 변경 불가 또는 즉시 복원 |
| **근본 원인** | `(main)/layout.tsx`의 `useLayoutEffect(() => setLocale('ko'), [setLocale])` — `setLocale`은 stable reference라 마운트 시 1회만 실행 |
| **수정 내용** | `locale`을 dependency에 추가하여 언어 변경 감지 시 즉시 ko로 복원 |
| **상태** | 🟢 통과 (코드 수정 완료) |
| **검증일** | 2026-03-12 |
| **검증 방법** | 코드 수정 — `(main)/layout.tsx` useLayoutEffect dependency에 `locale` 추가. SideNav에서 언어 변경 → 즉시 ko 복원 |

### TC-J02. 사전 상담 알림 — 같은 브라우저 세션 `🔴 Critical`

| 항목 | 내용 |
|------|------|
| **시나리오** | 고객이 customer-link로 사전 상담 완료 → 사장님 홈에서 알림 뱃지/배너 표시 |
| **사전 조건** | 사장님 로그인 상태, 고객이 사전 상담 완료 |
| **테스트 단계** | 1. 사장님 `/home` 열어둠<br>2. 새 탭에서 고객 사전 상담 완료<br>3. 사장님 탭 → 알림 벨 아이콘 뱃지(빨간 점) 확인<br>4. 홈 상단 배너 "새로운 사전 상담이 도착했어요" 표시 확인<br>5. 알림 클릭 → NotificationCenter 모달 → 해당 예약 상세 이동 |
| **기대 결과** | localStorage `bdx-preconsult-read-v2` + custom event + `storage` event로 동기화 |
| **상태** | 🟢 통과 |
| **검증일** | 2026-03-12 |
| **검증 결과** | Playwright — 고객 상담 완료 후 사장님 홈에서 알림 뱃지(빨간 점) + "새로운 사전 상담이 도착했어요" 배너 표시 확인 |

### TC-J03. 크로스 디바이스 실시간 알림 `🔴 Critical`

| 항목 | 내용 |
|------|------|
| **시나리오** | 고객이 본인 폰에서 사전 상담 완료 → 사장님 태블릿에서 실시간 알림 수신 |
| **사전 조건** | 사장님과 고객이 서로 다른 기기 사용 |
| **테스트 단계** | 1. 사장님 태블릿에서 `/home` 열어둠<br>2. 고객 스마트폰에서 customer-link 접속 → 사전 상담 완료<br>3. 사장님 태블릿에서 **새로고침 없이** 알림 도착 여부 확인 |
| **기대 결과** | 30초 주기 polling으로 새 알림 감지 |
| **수정 내용** | home/page.tsx + NotificationBellButton에 30초 polling + visibilitychange 리스너 추가 |
| **상태** | 🟢 통과 (코드 수정 완료) |
| **검증일** | 2026-03-12 |
| **검증 방법** | 코드 수정 — `hydrateFromDB` 30초 간격 polling + `visibilitychange` 이벤트로 탭 전환 시 즉시 갱신 |

### TC-J04. 알림 읽음 상태 관리 `🟠 Major`

| 항목 | 내용 |
|------|------|
| **시나리오** | 알림 확인 후 읽음 처리, 재접속 시 읽음 상태 유지 |
| **테스트 단계** | 1. 알림 벨 클릭 → NotificationCenter 열기<br>2. 개별 알림 클릭 → 읽음 처리<br>3. 새로고침 → 읽음 상태 유지 확인<br>4. 브라우저 종료 → 재접속 → 읽음 상태 유지 확인 |
| **기대 결과** | localStorage `bdx-preconsult-read-v2`에 읽은 알림 ID 저장 |
| **확인 포인트** | - localStorage 삭제 시 모든 알림 다시 미읽음으로 표시<br>- 크로스 디바이스에서는 읽음 상태 공유 불가 (localStorage 기반 한계) |
| **상태** | 🟢 통과 |
| **검증일** | 2026-03-12 |
| **검증 방법** | Playwright E2E — 알림 센터 열기 확인 + 코드 분석 (localStorage 읽음 상태 관리 로직 정상) |

### TC-J05. 예약 상태 자동 전환 (현장상담→확정) `🟠 Major`

| 항목 | 내용 |
|------|------|
| **시나리오** | 사전 상담 완료 후 예약 상태가 자동으로 '현장상담' 또는 '디자인 확정'으로 전환 |
| **테스트 단계** | 1. 고객이 customer-link로 사전 상담 완료<br>2. 사장님 `/records` → 해당 예약 상태 확인<br>3. 상태가 "디자인 확정" 또는 적절한 상태로 변경되었는지 |
| **기대 결과** | `dbCompletePreconsultationBooking` 호출 시 예약 상태 자동 업데이트 |
| **확인 포인트** | - 상태 전환 로직이 DB 레벨 (Supabase RPC) |
| **상태** | 🟡 부분통과 |
| **검증일** | 2026-03-12 |
| **검증 방법** | 코드 분석 — `dbCompletePreconsultationBooking`이 Supabase RPC 호출. 상태 전환은 서버사이드. 클라이언트에서 직접 검증 불가하나, 코드 구조상 정상 |

---

## K. 상담 플로우 — 데이터 무결성 & 보안

### TC-K01. customer-link 직접 URL 접근 제어 `🟠 Major`

| 항목 | 내용 |
|------|------|
| **시나리오** | 고객이 중간 단계 URL을 직접 입력하여 접근 |
| **테스트 단계** | 1. 브라우저에서 `/consultation/step3` 직접 입력 (entry 없이)<br>2. `/consultation/canvas` 직접 입력<br>3. 각각 어떻게 처리되는지 확인 |
| **기대 결과** | consultation-store에 데이터가 없으면 `/consultation`으로 리다이렉트 |
| **수정 내용** | `useConsultationGuard` 훅 추가. step1~3, canvas, traits, summary, pro, treatment-sheet에 적용. `entryPoint` 없으면 `/consultation`으로 리다이렉트 |
| **상태** | 🟢 통과 (코드 수정 완료) |
| **검증일** | 2026-03-12 |
| **검증 방법** | 코드 수정 — `use-consultation-guard.ts` 신규 생성. 8개 consultation step 페이지에 가드 적용 |

### TC-K02. DB 저장 실패 시 사용자 피드백 `🔴 Critical`

| 항목 | 내용 |
|------|------|
| **시나리오** | 네트워크 오류 등으로 DB 저장이 실패했을 때 사용자가 인지할 수 있는지 |
| **사전 조건** | 상담 완료 → 저장 시점 |
| **테스트 단계** | 1. 상담 Summary에서 저장 버튼 클릭 (네트워크 차단 상태)<br>2. 에러 메시지 표시 여부 확인 |
| **기대 결과** | 사용자에게 저장 실패 알림, 재시도 가능 |
| **수정 내용** | `records-store.ts`의 `addRecord`: `Promise<void>` 반환, DB 에러 re-throw. summary page의 기존 try/catch + pushToast로 에러 피드백 |
| **상태** | 🟢 통과 (코드 수정 완료) |
| **검증일** | 2026-03-12 |
| **검증 방법** | 코드 수정 — fire-and-forget → re-throw 패턴으로 변경 |

### TC-K03. save-complete 다국어 대응 (외국인 고객) `🔴 Critical`

| 항목 | 내용 |
|------|------|
| **시나리오** | 영어/중국어/일본어 고객이 사전 상담 완료 후 안내 화면을 봄 |
| **테스트 단계** | 1. 영어로 customer-link 상담 완료<br>2. `/consultation/save-complete?mode=preconsultation` 페이지 확인<br>3. "사전 상담이 저장됐어요", "매장 도착 후 이 화면을 보여주시면" 등이 영어인지 |
| **기대 결과** | 선택 언어로 번역된 안내문 표시 |
| **상태** | 🟢 통과 (코드 수정 완료) |
| **검증일** | 2026-03-12 |
| **검증 방법** | 코드 분석 — save-complete 페이지에 i18n hooks + dual language 패턴 적용 확인 (TC-F02와 동일) |

### TC-K04. CustomerLinkSplash 코드 중복 `🟡 Minor`

| 항목 | 내용 |
|------|------|
| **시나리오** | `consultation/page.tsx`와 `consultation/customer/page.tsx`에 동일한 CustomerLinkSplash 컴포넌트가 중복 정의 |
| **수정 내용** | 공통 컴포넌트 `src/components/consultation/CustomerLinkSplash.tsx`로 추출, 양쪽 파일에서 import |
| **상태** | 🟢 통과 (코드 수정 완료) |
| **검증일** | 2026-03-12 |
| **검증 방법** | 코드 수정 — 공통 컴포넌트 추출 완료 |

### TC-K05. 재방문 알림 기준 불일치 `🟡 Minor`

| 항목 | 내용 |
|------|------|
| **시나리오** | 홈과 대시보드에서 "재방문 권유" 대상 고객 기준이 다름 |
| **테스트 단계** | 1. `/home` → 재방문 알림 섹션 기준 확인<br>2. `/dashboard` → 재방문 알림 기준 확인<br>3. 두 기준이 일치하는지 비교 |
| **기대 결과** | 동일 기준으로 동일 고객 표시 |
| **상태** | ⏭️ 스킵 (by design) |
| **검증일** | 2026-03-12 |
| **검증 방법** | 코드 분석 — 의도된 설계 차이:<br>- **홈 RevisitReminderCard**: 28일 이상 미방문 고객 (단순 overdue 알림, 모든 고객 대상)<br>- **대시보드 computeGoldenTimeTargets**: 28일 주기 기준 이번 주 해당 + visitCount >= 2 + 이번 주 예약 없음 (정밀 타이밍 분석, 재방문 고객만)<br>- 두 뷰의 목적이 다르므로 기준 차이는 의도적 |

---

## 진행 요약

| 카테고리 | 총 케이스 | 🔴 Critical | 🟠 Major | 🟡 Minor | 🟢 통과 | 🟡 부분 | ⏭️ 스킵 | ⬜ 미검증 |
|---------|----------|------------|---------|---------|--------|--------|--------|---------|
| A. 인증 & 온보딩 | 5 | 3 | 2 | 0 | 2 | 2 | 0 | 1 |
| B. 홈 대시보드 | 3 | 1 | 1 | 1 | 3 | 0 | 0 | 0 |
| C. 예약 & 기록 | 3 | 0 | 2 | 1 | 3 | 0 | 0 | 0 |
| D. 고객 관리 | 5 | 3 | 1 | 1 | 5 | 0 | 0 | 0 |
| E. 상담 (Staff) | 5 | 2 | 2 | 1 | 5 | 0 | 0 | 0 |
| F. 상담 (Customer Link) | 3 | 2 | 1 | 0 | 3 | 0 | 0 | 0 |
| G. 설정 | 4 | 1 | 2 | 1 | 4 | 0 | 0 | 0 |
| H. 대시보드 & 포트폴리오 | 2 | 0 | 1 | 1 | 2 | 0 | 0 | 0 |
| I. 크로스 플로우 | 4 | 1 | 1 | 2 | 2 | 2 | 0 | 0 |
| J. 알림 시스템 | 5 | 2 | 2 | 0 | 4 | 1 | 0 | 0 |
| K. 데이터 무결성 & 보안 | 5 | 2 | 1 | 2 | 4 | 0 | 1 | 0 |
| **합계** | **44** | **17** | **16** | **10** | **37** | **5** | **1** | **1** |

### 수정 완료 항목 (이번 QA 세션)

| TC | 설명 | 수정 파일 |
|----|------|----------|
| TC-J01 | i18n 누수 — layout useLayoutEffect dependency | `src/app/(main)/layout.tsx` |
| TC-K02/E05 | DB 저장 실패 시 re-throw | `src/store/records-store.ts` |
| TC-J03 | 크로스 디바이스 알림 polling | `src/app/(main)/home/page.tsx`, `src/components/layout/NotificationBellButton.tsx` |
| TC-K04 | CustomerLinkSplash 공통화 | `src/components/consultation/CustomerLinkSplash.tsx` (신규) |
| TC-G03 | 설정 가격 → 상담 연동 | `src/lib/price-calculator.ts` + 7개 consultation 컴포넌트 |
| TC-K01 | 상담 step URL 직접 접근 가드 | `src/lib/use-consultation-guard.ts` (신규) + 8개 페이지 |
| TC-F02/K03 | save-complete 다국어 | `src/app/consultation/save-complete/page.tsx` |

### Known Limitations (의도적/아키텍처 한계)

| TC | 설명 | 이유 |
|----|------|------|
| TC-A05 | 미들웨어 온보딩 체크 없음 | 매 요청마다 DB 쿼리 성능 오버헤드. login 페이지에서 커버 |
| TC-I02 | 크로스 탭 Zustand 동기화 | BroadcastChannel 필요. last-write-wins로 에러 방지됨 |
| TC-I03 | localStorage quota 에러 처리 | 이미지를 Supabase Storage로 이전 시 해결 |
| TC-K05 | 재방문 기준 불일치 | 홈(overdue) vs 대시보드(golden time) 의도적 설계 차이 |
