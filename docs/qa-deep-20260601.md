# QA Deep — 2026-06-01 클라이언트 피드백 5건

지승호 대표 iPad 실측 피드백 5건 조사·수정·검증. 원인 추적은 코드+Supabase 실데이터로 확정.

## 요약

| # | 이슈 | 원인 | 수정 | 검증 |
|---|------|------|------|------|
| 1 | 회원권 "만료" 오표시 + 수정 안 됨 | jsonb `status:'expired'` 박힘(미래 만료일·만액인데). 수정 시 기존 status 보존 → 안 풀림 | 수정 시 잔액>0이면 `active` 재활성화 / 지승호 레코드 status→active 패치 | DB status=active 확인 ✅ |
| 5 | 회원권 상품 추가 실패 | `membership_plans_total_sessions_check CHECK(>0)`가 금액권(total_sessions=0) INSERT 차단 | CHECK를 `>= 0`으로 완화 + DEFAULT 0 (마이그레이션) | 30만원 금액권 INSERT 통과 ✅ |
| 2 | 메뉴 미설정 사진이 현장시술에 노출 | field-mode 디자인 선택이 `isPublic`만 필터, 메뉴(isFeatured) 미적용 | `isFeatured===true` 필터 추가 (빈 상태=카테고리만으로 시작 fallback 유지) | build ✅ |
| 3 | 공유카드/공유링크 불일치 + '너무 만족' 평 사라짐 + 폰트 일그러짐 | 다운로드 이미지(ShareCardImageTemplate)와 공개 페이지(ShareCardClient)가 별도 구현 → 디자인·폰트 다름, 이미지엔 💕 만족 행 없음 | ShareCardImageTemplate을 SSOT로 통일 — 공개 페이지가 동일 템플릿 재사용(`ShareCardScaledPreview`). 💕 만족 행 추가, 영문 타이틀+한글 서브 통일, QR CTA i18n(`reserveCta` 4개 언어) | Chrome 실측: 공개 페이지가 동일 카드 렌더(💕 너무 만족하셨어요·QR·로고·폰트 일치) ✅ |
| 4A | 스케줄 '시간 미정' (지브로 테스트) | 직접접근(경로 C) 사전상담이 `reservation_time:'미정'`(한국어 리터럴) 하드코딩 | 빈 값 저장 + 리스트/바텀시트가 styled '시간 미정' 라벨로 표시(레거시 '미정'도 동일 처리) | build ✅ |
| 4B | 캘린더 우상단 버튼 겹침 | 월간뷰 일간 토글이 `absolute top-2 right-2`로 MonthCalendar '다음 달' 버튼과 포개짐 | 토글을 캘린더 위 별도 행(라벨 '일간 보기')으로 분리 | build ✅ |

## 변경 파일

- **DB 마이그레이션(라이브 적용)**: `relax_membership_plans_total_sessions_check` — total_sessions CHECK `>0`→`>=0`, DEFAULT 0
- **DB 데이터 패치(라이브)**: 지승호(customer-1780027549486) membership.status `expired`→`active`
- `src/app/(main)/customers/[id]/page.tsx` — 회원권 수정 시 잔액>0 → active 재활성화
- `src/app/field-mode/page.tsx` — 디자인 선택 isFeatured 필터
- `src/components/share-card/ShareCardImageTemplate.tsx` — props를 discrete 필드로, 💕 행 추가, 타이틀 통일, 이미지 없을 때 img 생략, `ShareCardScaledPreview` export
- `src/components/share-card/ShareCardGeneratorModal.tsx` — 새 props 적용, 로컬 ScaledPreview→공유 컴포넌트, dead memo 제거
- `src/app/share/[shareCardId]/ShareCardClient.tsx` — 손코딩 카드→공유 SSOT 템플릿(미리보기+숨김 풀사이즈 캡처)
- `src/lib/db.ts` — dbCompletePreConsultation reservation_time `'미정'`→`''`
- `src/components/calendar/DayReservationList.tsx` — 시간 미정 styled 라벨
- `src/app/(main)/records/page.tsx` — 월간뷰 일간 토글 레이아웃 분리
- `src/lib/i18n.ts` — `shareCard.reserveCta` 4개 언어 추가

## 검증

- `npx tsc --noEmit` EXIT 0
- `pnpm lint` 경고만(사전 존재 img/useMemo), 에러 없음
- `pnpm build` EXIT 0 (37 라우트)
- Chrome 실측: 공개 공유 페이지(`/share/cb36fdc4…`) 통일 카드 + 💕 만족 행 렌더 확인
- DB: 이슈5 INSERT 통과 / 이슈1 status=active

## 비고

- 이슈 5(상품 추가)·이슈 1(지승호 만료 표시)는 **DB 변경이 이미 라이브** → 프로덕션에서 바로 검증 가능.
- 이슈 1(수정 재활성화)·2·3·4는 **코드 변경** → 배포 필요. 인증 페이지라 무로그인 Chrome QA 불가, build+코드검증으로 확인.
