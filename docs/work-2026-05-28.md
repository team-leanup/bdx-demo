# BDX 작업 일지 — 2026-05-28

## 요약

| 영역 | 커밋 | 미커밋 |
|------|------|--------|
| 푸시된 작업 | 20건 (b6eb20f까지) | — |
| 미푸시 코드 변경 | — | 30개 파일 |
| 발견 → 수정 | 17건 | (28건 완료, LOW 2건 보류) |
| 보안 마이그레이션 대기 | — | 4건 (DB 변경 필요) |

---

## 1. 커밋·푸시 완료

### 1-1. 커스텀 파츠 카운터 — 손님이 사전상담에서 개수 선택 가능 (`867ceb5`, `d956005`)

손님이 사전상담 추가 옵션에서 사장님이 등록한 커스텀 파츠(큐빅·스와로브스키 등)를 +/- 버튼으로 개수 직접 선택. 가격 계산·요약·confirm 페이지 모두 연동.

- `pre-consult-store.ts` — `customPartSelections` 상태 + `incrementCustomPart` / `decrementCustomPart` 액션
- `AdditionalOptions.tsx` — +/- 버튼 + ×N 카운터 + 파츠별/섹션 합계
- `pre-consult-price.ts` — `customPartsSurcharge` 별도 필드로 계산
- `confirm/page.tsx` — 요약 + 가격 라인 분리 표시 + submit에 포함
- `records/preconsult/[bookingId]/page.tsx` — 사장님 뷰에서도 표시

**가격 계산 정합성 보강**:
- 현장모드 메인 PriceBar — `customPartCounts`를 가격 계산에 전달 → 예상 가격에 합산
- `records/preconsult` 사장님 뷰 — 사전상담 손님이 선택한 커스텀 파츠 가격 반영
- `settlement` / `treatment` 페이지는 `inTreatmentAddons`로 별도 합산하므로 중복 방지 위해 미변경

### 1-2. QA 전수조사 후속 11건 일괄 (`b6eb20f`)

5개 에이전트 병렬 점검 → 정합성·보안·접근성·i18n 11건 수정.

**데이터 정합성 (HIGH 2)**
- `settlement` 정산 시 `customPartSelections` 누락 → `hydrateFromBooking`에 `initialInTreatmentAddons` 추가, 손님 사전상담 선택 파츠가 시술 시작 시 자동 채워져 정산 금액에 반영
- `records/preconsult` 사장님 뷰에 손님이 선택한 커스텀 파츠 칩 표시 (이름 ×개수 +금액)

**보안 (CRITICAL 2 + HIGH 1)**
- `uploadPreConsultImage`: MIME 화이트리스트 + 확장자 검증 + 10MB 크기 제한
- `customPartsSurcharge` count 음수/소수 차단 (`Math.max(0, Math.floor)` 클램프)
- `pre-consult-store` partialize에서 `selectedPhotoUrl` base64 persist 차단 (QuotaExceeded 재발 방지)

**접근성 (CRITICAL 2 + HIGH 1)**
- +/- 버튼 클릭 영역 `-inset-3`으로 48px 확장 (시각 24px 유지)
- `aria-label` 명확화 ("1개 추가/감소") + `aria-pressed` 추가
- `RevisitMessageSection` 변수 pill `min-h-36` → `44px`

**타입 안전성 (HIGH 1)**
- `AddOnOption`에 `'wrapping'` 추가 + `ADDON_FIXED_PRICES.wrapping(5000)` + 가격 계산 분기 추가
- `QuickOptionsPanel`의 `'wrapping' as AddOnOption` 강제 캐스팅 제거
- 랩핑 선택해도 가격 미반영되던 버그 동시 해결

**i18n / 코드 품질**
- `AdditionalOptions` `/개` → `t('preConsult.perUnit')`, `partsListTitle` dual language 보조 표시
- `dashboard.title` zh/ja 누락 추가
- `OptionBtn`/`AddOnChip` `type="button"` 추가 (form submit 사고 방지)
- `records/preconsult/[bookingId]` images 배열 mutation 제거

---

## 2. 미커밋 (30개 파일)

### 2-1. 시술 종류(DesignCategory) 동적화 — 사장님이 추가 가능

기본 4개(심플/프렌치/자석/아트) 외에 사장님이 최대 4개 추가 가능 (총 8개). 사전상담·현장모드·포트폴리오·설정 모두 연동.

**사용자 결정 사항**
- 기본 4개는 유지 + 추가만 가능
- 한국어 필수 + en/zh/ja 선택 (미입력 시 한국어 fallback)
- 워딩 "시술 종류"로 통일
- 최대 8개

**핵심 변경 파일**
- `src/types/shop.ts` — `CustomCategory` 인터페이스
- `src/types/pre-consultation.ts` — `BuiltinDesignCategory` + `DesignCategory = BuiltinDesignCategory | string`
- `src/lib/category-resolver.ts` (NEW) — 동적 라벨·가격·이름 조회 헬퍼
- `src/lib/pre-consult-price.ts` — `customCategories` 옵션 추가
- `src/lib/design-category-guard.ts` — `asDesignCategory(value, shopSettings)` custom id 허용
- `src/lib/category-mapping.ts` — `serviceTypeToCategory` 'custom-' prefix 통과
- `src/lib/db.ts` — `fetchShopPublicData` 응답에 `customCategories` 포함
- `src/store/app-store.ts` — `ShopSettings.customCategories` + persist merge 보존
- `src/app/(main)/settings/page.tsx` — "시술 종류" UI, 추가/편집/삭제 + 다국어 입력
- `src/components/pre-consult/CategoryPicker.tsx` — builtin + custom 그리드
- `src/components/portfolio/UploadPhotoForm.tsx`, `EditPhotoModal.tsx` — 드롭다운에 custom 옵션
- `src/app/field-mode/page.tsx`, `src/components/field-mode/PortfolioGrid.tsx` — `CATEGORY_TABS` 동적
- `src/app/field-mode/treatment|settlement|wrap-up/page.tsx` — `CATEGORY_LABELS` → `resolveCategoryLabelKo`
- `src/app/(main)/records/preconsult/[bookingId]/page.tsx` — "시술 종류" 워딩 + custom fallback
- `src/components/pre-consult/ConsultReview.tsx` — IIFE로 builtin/custom 분기
- `src/app/pre-consult/[shopId]/design/page.tsx` — `VALID_CATEGORIES` 동적 검증

**코드 리뷰 후속 핫픽스 4건**
- `CATEGORY_TO_SCOPE` custom id → 'solid_tone' fallback (`category-mapping.ts`)
- `app-store` persist merge에 `customCategories` / `customParts` / `depositAmount` 추가
- 설정 추가 카테고리 id `crypto.randomUUID()`로 충돌 방지
- 편집 진입 시 `customCategoriesEdit` 재초기화 (stale state 방지)

### 2-2. Chrome 직접 QA → 발견 17건

8개 영역(예약·매출·시술·사전상담·기록·대시보드·고객·설정) 시나리오를 손님·사장님 시점으로 모두 진행.

| # | 우선순위 | 발견 |
|---|---------|------|
| #1 | HIGH | 새 예약 시술 종류 옵션 `원컬러/그라데이션/자석젤/아트` — 다른 곳과 워딩 불일치, 프렌치 누락 |
| #2 | HIGH | 매출/예약/포트폴리오 시술 종류 옵션 갯수 페이지마다 다름 (4·5·9개) |
| #3+4 | CRITICAL | 즉시 매출 `customer_id ''` (빈 문자열) → FK 위반. 워크인 손님 매출 등록 불가 |
| #6 | HIGH | 현장모드 사진 가격 ₩30,000 표시되지만 디자인 확정 모달에서 카테고리 기본가 ₩60,000로 표시 |
| #8 | LOW | 사전상담 슬롯 "16자리/13자리" 표기 UX (보류) |
| #9 | LOW | 가격 표기 형식 일관성 (`₩30,000` vs `30,000원`) (보류) |
| #10 | HIGH | NailStatusSelector `showRemoval` 잔여 state — 'none' 선택해도 오프 질문 잔존 |
| #11 | MED | 사전상담 흐름 막힘 (잔여 state로 해결됨) |
| #12 | CRITICAL | `preConsult.perUnit` raw key 손님 화면 노출 (perUnit이 selector namespace에만 존재) |
| #13 | HIGH | 사전상담 제출 후 records 캘린더 시간그리드에 미표시 (헤더 카운트만) |
| #13(대시보드) | MED | 사전상담 1건이 "이달 상담 건수" 미카운트 |
| #15 | HIGH | 사전상담 제출+확정한 손님 자동 고객 등록 안 됨 |
| #16 | MED | 0회 방문 신규 고객 자동 "단골" 태그 |

### 2-3. CRITICAL → MED 수정 9건

| 수정 | 파일 |
|------|------|
| `preConsult.perUnit` 4개 언어 추가 | `i18n.ts` |
| customer FK 위반 차단 — 워크인 손님 자동 customer 생성 + 빈문자열 차단 | `records-store.ts`, `db.ts` |
| NailStatusSelector `showRemoval` 동기화 — `handleSelect('none')`에서 명시적 false | `NailStatusSelector.tsx` |
| 디자인 확정 모달이 `photo.price` 우선 사용 (없으면 카테고리 기본가) | `DesignConfirmSheet.tsx` |
| 시술 종류 옵션 통일 — `심플/프렌치/자석/아트` + `customCategories` | `ReservationForm.tsx`, `QuickSaleForm.tsx` |
| records 페이지 mount 시 `hydrateFromDB` 자동 호출 → 사전상담 booking 시간그리드 노출 | `records/page.tsx` |
| 사장님 "확정하기" 시 customer 자동 매칭/생성 + booking.customerId 업데이트 | `preconsult/[bookingId]/page.tsx` |
| `computeMonthlyConsultations`에 reservations 추가 — 사전상담도 카운트 | `analytics.ts`, `dashboard/page.tsx` |
| 신규 고객 `isRegular: false` 명시 — 자동 단골 표시 제거 | `customer-store.ts`, `reservation-store.ts` |

---

## 3. 남은 작업

### 3-1. LOW 2건 (보류 — 사용자 요청 범위 외)
- 발견 #8: 사전상담 슬롯 "16자리/13자리" 표기 UX
- 발견 #9: 가격 표기 형식 통일 (`₩30,000` vs `30,000원`)

### 3-2. 보안 4건 (DB 마이그레이션 필요 — 사용자 결정 대기)

| 우선순위 | 항목 | 작업 |
|---------|------|------|
| HIGH | `pre_consultations` 테이블 RLS 미설정 (anon 전체 조회/삭제 가능) | DB 마이그레이션 |
| HIGH | `booking_requests.deposit` anon 조작 가능 | DB 트리거 추가 |
| HIGH | `bdx-demo` 쿠키 `HttpOnly` 누락 (XSS → 인증 우회) | 미들웨어/auth-store 분리 |
| MED | `membership_plans_demo_open FOR ALL` (anon DELETE 가능) | RLS 정책 `FOR SELECT` 제한 |

### 3-3. 미커밋 변경
- 30개 파일 (위 2-1 ~ 2-3 전부) — 커밋·푸시 대기

---

## 4. 변경 파일 목록 (미커밋 30개)

```
src/app/(main)/dashboard/page.tsx
src/app/(main)/home/page.tsx
src/app/(main)/records/page.tsx
src/app/(main)/records/preconsult/[bookingId]/page.tsx
src/app/(main)/settings/page.tsx
src/app/field-mode/page.tsx
src/app/field-mode/settlement/page.tsx
src/app/field-mode/treatment/page.tsx
src/app/field-mode/wrap-up/page.tsx
src/app/pre-consult/[shopId]/confirm/page.tsx
src/app/pre-consult/[shopId]/design/page.tsx
src/components/calendar/DayReservationList.tsx
src/components/field-mode/DesignConfirmSheet.tsx
src/components/field-mode/PortfolioGrid.tsx
src/components/field-mode/PriceBar.tsx
src/components/home/ReservationForm.tsx
src/components/payment/QuickSaleForm.tsx
src/components/portfolio/EditPhotoModal.tsx
src/components/portfolio/UploadPhotoForm.tsx
src/components/pre-consult/CategoryPicker.tsx
src/components/pre-consult/ConsultReview.tsx
src/components/pre-consult/NailStatusSelector.tsx
src/lib/analytics.ts
src/lib/category-mapping.ts
src/lib/category-resolver.ts  (NEW)
src/lib/db.ts
src/lib/design-category-guard.ts
src/lib/i18n.ts
src/lib/pre-consult-price.ts
src/store/app-store.ts
src/store/customer-store.ts
src/store/field-mode-store.ts
src/store/pre-consult-store.ts
src/store/reservation-store.ts
src/types/pre-consultation.ts
src/types/shop.ts
src/components/pre-consult/AdditionalOptions.tsx
src/components/field-mode/QuickOptionsPanel.tsx
```

---

## 5. 검증
- `npx tsc --noEmit` ✅
- `pnpm build` ✅
- 푸시 안 함 — 사용자 별도 지시 대기
