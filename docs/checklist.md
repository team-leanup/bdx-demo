# BDX 개발 체크리스트

> 클라이언트 피드백 기반. 최종 업데이트: 2026-03-10

**상태**: ⬜ 미착수 · 🟡 진행중 · 🔵 부분완료 · 🟢 완료 · 🔴 블로커

---

## 1. 제품 방향 전환 — "상담 시스템 → 고객 CRM"

> 핵심: BDX는 ERP가 아니라 **고객 성향 관리 + 시술 기록 CRM**

### 1.1 ⬜ CLAUDE.md 제품 설명 업데이트
- **지금**: "네일 샵 상담 플로우 앱"으로 소개됨
- **할 일**: "네일 샵 고객 관리 CRM + 시술 기록 시스템"으로 변경. 핵심 기능 3가지(스케줄 CRM, 포트폴리오, 고객 성향 관리) 명시

---

## 2. 홈 화면 — 스케줄 중심으로 개편

> 핵심: 원장님이 앱 열면 **오늘 스케줄 + 고객 특징 태그**가 바로 보여야 함

### 2.1 🟢 홈 화면을 스케줄표 중심으로 변경
- **완료**: 스케줄표 중심 레이아웃 적용됨
- **완료 추가**: 기록 관리 페이지에서 `useShopStore((s) => s.getActiveDesigners())`처럼 새 배열을 반환하던 selector를 제거하고, 원본 `designers` 상태를 `useMemo`로 파생하도록 바꿔 React `getSnapshot` 무한 루프 경고를 해소함

### 2.2 🟢 예약 카드에 고객 특징 태그 표시
- **완료**: 예약 카드가 `customerId`로 고객의 주요 태그를 조회해 칩 형태로 최대 3개까지 표시함. pinned 태그가 있으면 우선 노출하고, 없으면 기존 고객 태그를 우선순위대로 보여줌
  ```
  김지영 · 자석젤
  [큐티클 민감] [오버레이 선호] [유지력 약함]
  ```
- **참고**: 시술 전 알림 모달은 기존처럼 pinned 태그만 사용하고, 카드 표시용 태그만 별도 우선순위 조회 로직으로 분리함

### 2.3 🟢 태그 색상 코딩
- **완료**: `CustomerTag.accent` 커스텀 색상을 유지하면서 카테고리별 기본 색상을 자동 적용하도록 공통 태그 컬러 시스템을 반영함. 스케줄 카드, 고객 상세, 예약 연결 모달, 시술 전 알림, 기록 상세에서 동일한 색상 규칙을 사용함

### 2.4 🟢 상담 준비 상태 아이콘
- **완료**: 예약별 사전 상담 완료 여부를 저장하고, 홈/예약 리스트/캘린더/예약 상세에서 `🟢 디자인 확정` / `🟡 현장 상담 필요` 상태 아이콘으로 표시함. 상담 링크 제출이 완료되면 자동으로 디자인 확정 상태로 전환됨

---

## 3. 포트폴리오 — 시술 사진 검색 시스템

> 핵심: "예전에 다른 손님이 했던 스타일" 요청 시 **사진으로 바로 찾고, 가격까지 확인**

### 3.1 🟢 포트폴리오 갤러리 강화
- **완료**: 포트폴리오 갤러리에 시술 종류/기간/가격 필터를 추가하고, 검색 대상을 고객명에서 태그·컬러·디자인 타입까지 확장함. 업로드 폼도 메타데이터 입력을 지원해 필터링 가능한 구조로 보강됨

### 3.2 🟢 사진 상세에서 시술 정보 + 가격 표시
- **완료**: 사진 상세 페이지에서 시술 정보 + 가격 표시 구현됨

### 3.3 🟢 포트폴리오 ↔ 고객 ↔ 상담 데이터 연결
- **완료**: 포트폴리오-고객-상담 간 데이터 연결 구현됨

### 3.4 🟢 대시보드에 인기 시술 사진 썸네일
- **완료**: 대시보드 `인기 시술 Top 3`를 실제 포트폴리오 `시술` 사진 기반 썸네일 카드로 교체하고, 클릭 시 대표 이미지·컬러번호·가격·고객·촬영일을 확인하는 팝업을 연결함. 포트폴리오 사진이 없을 때는 업로드 유도 빈 상태를 표시함

---

## 4. 고객 카드 — 성향 태그 관리

> 핵심: 고객 특성을 **기억이 아닌 데이터**로 관리. 강조·색상·고정 가능

### 4.1 🟢 고객 성향 태그 시스템
- **완료**: 고객 성향 태그 시스템 구현됨

### 4.2 🟢 태그 강조/고정 UI
- **완료**: 고객 상세 화면에서 태그별 `강조` 토글, 색상 선택, `상단 고정` 토글, 고정 태그 드래그 정렬 UI를 추가함. 편집 중에는 로컬 미리보기로 상단 특이사항/태그 순서가 즉시 반영되고, 저장 시 홈/예약 카드 우선 노출 순서까지 함께 갱신됨

### 4.3 🟢 VIP 비주얼 카드
- **완료**: VIP 비주얼 카드 구현됨

### 4.4 🟢 시술 이력 타임라인
- **완료**: 시술 이력 타임라인 구현됨

---

## 5. 시술 전 알림 — 고객 주의사항 미리 확인

> 핵심: 시술 시작 전 "이 고객은 큐티클이 민감합니다" 같은 **팝업 알림**

### 5.1 🟢 시술 전 알림 모달
- **완료**: `상담 시작` 전에 고객 주요 태그를 먼저 확인하는 시술 전 알림 모달을 홈/예약 리스트 흐름에 적용함. 태그별 주의 문구와 우선순위를 함께 표시해 확인 후 상담에 진입할 수 있음

### 5.2 🟢 세이프티 태그 색상 코드
- **완료**: 고객 태그 값을 기반으로 `🔴 높음 / 🟠 중간 / 🟡 참고 / 🟢 선호` 세이프티 레벨을 자동 분류하고, 시술 전 알림/예약 상세에서 동일한 색상과 안내 문구를 사용함

---

## 6. 상담 UX — 6단계로 간소화

> 핵심: 현재 상담이 복잡함. **최대한 빠르게 끝나야** 시술 가능 고객 수가 늘어남

### 6.1 🟢 상담 흐름 재구성
- **완료**: 상담 6단계 흐름 구현됨

### 6.2 🟢 기존 라우트 → 신규 라우트 마이그레이션
- **완료**: consultation/ 라우트 구조 전환됨

---

## 7. 가격 입력 — 시술 종료 후 확정

> 핵심: 네일 가격은 시술 중 변동이 많으므로, **시술 끝나고 최종 가격 입력**

### 7.1 🟢 시술 완료 후 가격 입력 화면
- **완료**: 시술 완료 후 가격 입력 화면 구현됨

### 7.2 🟢 가격 히스토리
- **완료**: 가격 히스토리 연동 구현됨

---

## 8. 상담 입력 방식 — 링크 발송 + 태블릿

> 핵심: 상담 데이터를 모으는 **2가지 경로** 제공

### 8.1 🟢 고객 상담 링크 발송
- **완료**: 상담 링크 발송 기능 구현됨

### 8.2 🟢 매장 태블릿 모드
- **완료**: 매장 태블릿 모드 구현됨

---

## 9. 캔버스 → 옵션 기능으로 분리

> 핵심: 손가락별 디자인 캔버스는 **모든 샵이 필요한 건 아님**

### 9.1 🟢 캔버스 옵션화
- **완료**: 캔버스가 옵션 기능으로 분리됨

---

## 10. 시술 리포트 기념품화 — 외국인 관광객 추억 콘텐츠 ⏳ MVP 이후

> 핵심: 시술 후 **"저장하고 자랑하고 싶은" 디지털 카드** 자동 생성
> **MVP 이후 구현 예정** — 핵심 CRM 기능 완성 후 진행

### 10.1 ⬜ 비주얼 다이어리 카드 `MVP 이후`
- 매거진 레이아웃으로 [시술 전 + 시안 + 시술 후] 배치. 이미지 저장 가능

### 10.2 ⬜ QR코드 리포트 접근 `MVP 이후`
- 시술 완료 시 QR코드 생성 → 고객 스캔 시 리포트 접근

### 10.3 ⬜ K-뷰티 인증 마크 / 샵 낙관 `MVP 이후`
- "Certified by BDX" 로고 + 샵 커스텀 낙관

### 10.4 ⬜ 퍼스널 케어 팁 (다국어) `MVP 이후`
- 피부톤 맞춤 컬러 추천 + 관리 팁 (ko/en/zh/ja)

### 10.5 ⬜ SNS 공유 (인스타 스토리용) `MVP 이후`
- 인스타 스토리 공유 버튼 + 샵 계정/해시태그 자동 삽입

### 10.6 ⬜ 비포/애프터 숏폼 영상 `MVP 이후`
- 15초 Reels/TikTok용 영상 자동 생성

### 10.7 ⬜ 친구 추천 할인 코드 (Referral) `MVP 이후`
- 리포트 하단 할인 코드 삽입

---

## 11. 대시보드 — 매출 중심 → 성과·현장 중심

> 핵심: 원장님이 **"BDX 덕분에 돈 더 벌었네"**를 체감하는 지표

### 11.1 🟢 업셀링 리포트
- **완료**: 대시보드 상단에 `업셀링 리포트` 와이드 카드를 추가해 상담 기반 디자인/표현기법/파츠/컬러 추가 매출을 별도 집계해 표시함. 업셀링 적용 상담 수, 상담당 평균 추가 매출, 가장 크게 기여한 옵션까지 함께 노출함

### 11.2 🟢 글로벌 대시보드
- **완료**: `오늘의 외국인 예약` 수와 언어별 예약 건수/준비 완료 건수/준비율을 한 카드에서 확인할 수 있도록 구현함. 예약 언어와 사전 상담 완료 상태를 실데이터 기반으로 집계해 BDX의 다국어 운영 강점을 시각화함

### 11.3 🟢 재방문 골든타임 알림
- **완료**: 평균 주기 28일 기준 이번 주 리마인더 대상 고객을 자동 선별해 리스트로 표시하고, 고객별 맞춤 리마인더 문구를 `문구 복사` 버튼으로 즉시 복사할 수 있게 구현함

### 11.4 🟢 디자이너 지표 고도화
- **완료**: `DesignerPerformance` 위젯에 디자이너별 매출, 예약 배정률, 상담 완료율, 완료 예약 건수를 추가하고, 상담 건수 차트와 함께 운영 지표용 바 차트를 확장함. 현재 데이터 구조상 `지정 예약률` 대신 확인 가능한 `예약 배정률`로 안전하게 반영함

---

## 12. 디자인 트렌드 분석

> 핵심: 샵 내 인기 디자인 랭킹으로 **포트폴리오 + 마케팅 강화**

### 12.1 ⬜ 인기 디자인 랭킹
- **지금**: 대시보드 "인기 시술 Top 3"이 하드코딩 더미 데이터
- **할 일**: 실제 상담/시술 데이터 기반으로 "이번 달 인기 디자인" 자동 산출. 포트폴리오 사진과 연동

### 12.2 ⬜ 트렌드 알림
- **할 일**: 랭킹에 오른 시술의 고객에게 알림 → 동기부여/재방문 유도

---

## 13. 인증 — 구글 로그인 + 실제 인증 시스템

> 핵심: 현재 **인증이 전혀 없음** (로그인 버튼 누르면 검증 없이 통과). 실서비스를 위해 구글 OAuth 필수

### 13.1 🔵 Supabase Auth 이메일 로그인 연결
- **지금**: `src/app/(auth)/login/page.tsx`, `src/app/(auth)/signup/page.tsx`, `src/store/auth-store.ts`가 Supabase Auth `signUp` / `signInWithPassword` / `getSession` 기반으로 실제 샵 계정 로그인을 처리함. 회원가입 시 빈 `shops` row와 owner `designers` row를 만들고, 이후 세션은 `currentShopId` 기준으로 복원됨
- **완료 추가**: `로그인 없이 서비스 체험하기` CTA는 이제 `demo@bdx.kr` Supabase 데모 계정으로 바로 로그인하고, `shop-demo` 샵의 seeded 데이터(고객/예약/기록/포트폴리오)로 진입함
- **완료 추가**: 배포 환경에서 Supabase 공개 env가 빠졌을 때는 placeholder 네트워크 호출 대신 명시적인 설정 오류 메시지를 반환하도록 auth bootstrap을 보강함
- **남은 일**: 이메일 인증 UX, 비밀번호 재설정, 에러 메시지 정교화, 서버 측 세션 검증 추가

### 13.2 🔵 구글 OAuth Provider 연동
- **지금**: `src/app/(auth)/login/page.tsx`, `src/app/(auth)/signup/page.tsx`, `src/store/auth-store.ts`에 Google OAuth 진입 버튼과 `signInWithOAuth` 흐름을 연결했고, `src/app/auth/callback/route.ts` + `src/middleware.ts`에서 Supabase SSR 기반 코드 교환/세션 복원을 처리함
- **완료 추가**: Google 로그인에서 아직 샵이 없는 계정은 `src/app/(auth)/signup/google/page.tsx`로 보내 이름/샵 이름을 입력받은 뒤 바로 샵 생성 + 온보딩으로 이어지도록 통합함
- **남은 일**: Supabase Dashboard의 Google provider에 Client ID/Secret 입력, Google Cloud Console에 `https://pzwmqorvrhdkckkdqemo.supabase.co/auth/v1/callback` 및 앱 callback URL 등록

### 13.3 ⬜ 미들웨어 인증 가드
- **지금**: `src/middleware.ts`가 보안 헤더만 추가하고 **인증 여부를 전혀 검증하지 않음**. 로그인 안 해도 모든 페이지 접근 가능
- **할 일**: NextAuth 세션 기반으로 미들웨어에서 토큰 검증. 비로그인 시 `/login`으로 리다이렉트. 공개 라우트(login, splash, intro)만 예외 처리

### 13.4 🔵 인증 상태 관리 전환
- **지금**: `useAuthStore`가 Supabase 세션을 `initializeAuth()` / `onAuthStateChange()`로 복원하고, `owner_id = auth.user.id` 기준으로 현재 샵/원장 프로필을 찾아 `currentShopId`, `activeDesignerId`, 온보딩 완료 여부를 세션 컨텍스트로 유지함
- **완료 추가**: 샵 row가 아직 없거나 연결되지 않은 세션은 `fetchShopByOwnerId().maybeSingle()`로 조용히 null 처리해, auth bootstrap 시 불필요한 콘솔 에러가 뜨지 않게 정리함
- **남은 일**: 현재는 owner 샵 계정 중심 인증만 지원함. 디자이너 개별 계정, 서버 검증 미들웨어, 권한 세분화는 추가 필요

---

## 14. 데이터베이스 — mock 데이터 → 실제 DB

> 핵심: 현재 **DB가 전혀 없고 모든 데이터가 localStorage**. 브라우저 초기화하면 데이터 전부 날아감

### 14.1 ⬜ DB 서비스 선정 및 연결
- **지금**: `package.json`에 DB 관련 패키지 없음 (Prisma, Drizzle, Supabase 등 전무). `.env` 파일도 없음. 모든 데이터가 `src/data/mock-*.ts` 하드코딩 → Zustand persist(localStorage)로 저장
- **할 일**: DB 서비스 선정 (Supabase PostgreSQL 추천 — 인증+DB+스토리지 통합) → 연결 설정 → `.env.local`에 DB URL 추가
- **대안**: PlanetScale(MySQL), Vercel Postgres, Neon 등

### 14.2 ⬜ DB 스키마 설계
- **지금**: 타입 정의만 존재 (`src/types/` — customer.ts, consultation.ts, portfolio.ts, auth.ts, shop.ts, price.ts 등)
- **할 일**: 기존 TypeScript 타입을 기반으로 DB 스키마 설계. ORM(Prisma 또는 Drizzle) 도입. 주요 테이블:
  - `users` — 디자이너/원장 계정 (구글 OAuth 연동)
  - `shops` — 매장 정보
  - `customers` — 고객 정보 + 태그
  - `consultations` — 상담 기록
  - `reservations` — 예약
  - `portfolio_photos` — 시술 사진
  - `treatments` — 시술 기록 + 가격

### 14.3 ⬜ API 라우트 구현
- **지금**: `src/app/api/` 라우트 없음. 모든 데이터 조작이 클라이언트 Zustand store에서 직접 처리
- **할 일**: Next.js App Router API 라우트(`app/api/`) 생성. CRUD 엔드포인트:
  - `/api/customers` — 고객 CRUD
  - `/api/consultations` — 상담 CRUD
  - `/api/reservations` — 예약 CRUD
  - `/api/portfolio` — 포트폴리오 CRUD
  - `/api/treatments` — 시술 기록 + 가격

### 14.4 ⬜ Zustand store → API 연동 전환
- **지금**: 6개 Zustand store가 각각 독립적으로 localStorage에 persist. 예:
  - `useCustomerStore` (`bdx-customers`) — mock 데이터 deepClone 후 localStorage
  - `useRecordsStore` (`bdx-records`) — 추가분만 persist, 조회 시 mock과 merge
  - `useReservationStore` (`bdx-reservations`) — 전체 persist
- **완료 추가**: Supabase hydrate 대상인 `shop`, `customers`, `consultation_records`, `booking_requests`, `portfolio_photos`는 현재 로그인된 `currentShopId` 기준으로만 읽고 쓰도록 스토어/DB helper를 정리해 샵 간 데이터가 섞이지 않게 함
- **할 일**: store의 데이터 소스를 localStorage → API fetch로 전환. SWR 또는 React Query 도입 검토. localStorage는 오프라인 캐시 용도로만 유지

### 14.5 🟢 이미지 스토리지
- **완료**: 포트폴리오 사진을 Supabase Storage `portfolio-images` 버킷에 업로드하고, 메타데이터는 `portfolio_photos` 테이블에서 관리하도록 전환함. 앱은 공개 URL 기반으로 이미지를 렌더링하고, 포트폴리오 스토어를 DB hydrate 흐름에 포함시킴
- **완료 추가**: 기존 `bdx-portfolio` localStorage base64 사진은 첫 hydrate 시 Supabase로 순차 이관하고, 업로드/삭제/초기화 결과를 사용자 피드백으로 노출함

---

## 15. 개발 도구 — Supabase MCP

> 핵심: 개발 워크스페이스에서 Supabase 프로젝트를 MCP로 안전하게 조회할 수 있어야 함

### 15.1 🟢 Supabase MCP 워크스페이스 연동
- **완료**: 루트 `.mcp.json`의 Supabase MCP 서버를 `read_only=true`로 고정해 기본 조회 전용으로 연결하고, 최초 브라우저 인증 및 `project_ref` 스코프 옵션을 개발 문서에 기록함

---

## 진행 요약

| 영역 | 항목수 | 🟢 | 🟡 | 🔵 | ⬜ |
|------|--------|-----|-----|-----|-----|
| 홈/스케줄 | 4 | 4 | 0 | 0 | 0 |
| 포트폴리오 | 4 | 4 | 0 | 0 | 0 |
| 고객 카드 | 4 | 4 | 0 | 0 | 0 |
| 시술 전 알림 | 2 | 2 | 0 | 0 | 0 |
| 상담 UX | 2 | 2 | 0 | 0 | 0 |
| 가격 입력 | 2 | 2 | 0 | 0 | 0 |
| 상담 입력 방식 | 2 | 2 | 0 | 0 | 0 |
| 캔버스 옵션화 | 1 | 1 | 0 | 0 | 0 |
| 리포트 기념품화 | 7 | 0 | 0 | 0 | 7 (MVP 이후) |
| 대시보드 | 4 | 4 | 0 | 0 | 0 |
| 트렌드 분석 | 2 | 0 | 0 | 0 | 2 |
| **인증** | **4** | **0** | **0** | **2** | **2** |
| **데이터베이스** | **5** | **1** | **0** | **0** | **4** |
| 개발 도구 | 1 | 1 | 0 | 0 | 0 |
| **합계** | **45** | **27** | **0** | **2** | **16** |
