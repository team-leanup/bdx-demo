# 심층 전수 감사 — 2026-05-30

코드베이스·DB(Supabase)·API·플랫폼 로직(사용자 관점)을 9개 축으로 병렬 조사 + 축별 적대적 검증
후, 확정 이슈를 수정하고 Chrome 실측 + ui-ux-pro-max 검수 + 프로덕션 빌드로 검증.

## 방법
- **조사**: 9축(인증/온보딩, 상담플로우, 사전상담/슬롯, 기록/결제/회원권, 현장모드, 대시보드/통계, 포트폴리오/공유카드, i18n, DB/RPC/RLS) 병렬 조사 → 축별 HIGH/CRITICAL 적대적 재검증
- **검증**: 거짓양성 5건 기각(handleFinalize 이중호출, QuickSale 회원권, isSaving 잠김, customParts baseEstimate, CategoryPicker 빈문자열) — 코드 재확인으로 무효 판정
- **실측**: Chrome 으로 홈/기록/고객/대시보드/설정 + 상담 저장 end-to-end + 사전상담 공개 플로우 직접 테스트, Supabase MCP 로 DB 검증
- **결과**: `tsc --noEmit` 통과 · `next build` 통과(exit 0) · lint 에러 0

## 수정 완료 (확정·검증)

### 보안 / DB (마이그레이션 `20260530_audit_fixes.sql`)
| # | 심각도 | 내용 |
|---|--------|------|
| S1 | HIGH | `complete_preconsultation_for_booking` 레거시 4인자 오버로드 DROP — shop 검증 없이 anon 이 임의 booking 의 사전상담 데이터를 덮어쓸 수 있던 취약점. 5인자(p_shop_id 검증) 버전만 유지. `database.ts` 타입 보정 + `as never` 제거 |
| S2 | HIGH | `get_bdx_shop_stats`(owner_email PII)·`get_bdx_admin_stats`(전체 지표)·`rls_auto_enable` 의 EXECUTE 를 PUBLIC/anon/authenticated 에서 회수, service_role 만 유지 |
| S3 | HIGH | 공유 상담 링크 슬롯 중복 예약 방지 — `uq_booking_link_slot` 부분 UNIQUE 인덱스 + `dbCreateBookingFromConsultationLink`/`dbCreateBookingFromShopLink` 사전 충돌검사 + 23505 감지 → `slot_taken` 반환, 손님 화면 안내 |
| S4 | MEDIUM | OAuth 콜백 `getSafeNextPath` path traversal — `new URL` 정규화 후 allowlist 비교 |
| S5 | MEDIUM | 데모 쿠키 substring 우회(`includes`) → 정확 매칭(`split(';').some`) |
| S6 | MEDIUM | `dbUpdateCustomerField` shopId 필수화(교차 샵 수정 차단) |

### 데이터 정합성
| # | 심각도 | 내용 |
|---|--------|------|
| D1 | HIGH | 공유카드 shops 조회를 `get_shop_public_data` RPC 경유로 변경 — anon RLS 차단으로 **비-demo 샵 공유카드가 항상 404 나던 버그** 해결 |
| D2 | HIGH | 공유카드 포트폴리오 사진에 `is_public=true` 필터 — 비공개 사진 URL 공개 노출 차단 |
| D3 | HIGH | 캔버스 파츠가 가격에 미반영 — `canvas` handleChange 에서 `setPartsSelections/setHasParts` 동기화 |
| D4 | HIGH | customer_link 상담 저장 후 잘못된 레코드(`record-001`)로 이동 — `consultationId` 전달 + 누락 시 `/home` |
| D5 | HIGH | 결제 재진입 시 회원권 이중 차감/통계 이중 누적 — `record.paymentMethod` 가드 + 버튼 disabled + `useMembershipSession` recordId 가드 |
| D6 | HIGH | 회원권 결제 consultation 레코드 삭제 시 rollback 누락 — 결제 시 `membershipApplied` 저장 |
| D7 | HIGH | 현장 정산 DB 저장 실패해도 booking 이 completed 로 변경 — `recordSaved` 조건 안으로 이동 |
| D8 | HIGH | 현장 정산 회원권 차감 guard 불일치(만료 회원권 차감 가능) — `isMembershipPayment` 로 통일 + 고객 변경 시 stale 초기화 |
| D9 | MEDIUM | 신규 고객 `dbUpsertCustomer` 이중 호출 분리 / `deposit`·`pricingAdjustments` 레코드 추적 / `pre_consultations.booking_id` 역방향 링크 / `dbUpsertReservation` deposit 보존 |
| D10 | MEDIUM | 소급 매출 등록 시 `lastVisitDate` 가 항상 오늘로 덮어써짐 → 실제 시술일 반영(재방문 알림 정확도) |
| D11 | MEDIUM | 데모 고객 이름중복 dedup 으로 customerId dangling → id 기반 merge |

### 로직 / 통계
| # | 심각도 | 내용 |
|---|--------|------|
| L1 | HIGH | **슬롯 off-by-one** — 마감에 정확히 끝나는 마지막 슬롯이 매일 누락(`generateSlotCandidates`의 strict `<`). `<=` 로 수정. **Chrome 실측 검증**: 토 13→14, 평일 17→18, 금 19→20자리 |
| L2 | HIGH | paused 상태 공유링크에서도 예약 가능 → `status !== 'active'` 차단(클라이언트 + computeAvailableDates) |
| L3 | HIGH | KPI '오늘 매출' 바텀시트가 `membershipApplied` 누락 → KPI 카드와 불일치 수정 |
| L4 | HIGH | `computeTodayBookings` 취소 예약 포함 → `status !== 'cancelled'` 필터 |
| L5 | MEDIUM | 통계 timezone(로컬 getMonth/getFullYear) → KST(`getTodayInKorea`) 기준. weekStats/getByWeek 날짜 파싱 KST 보정 |
| L6 | MEDIUM | `aggregateMonthly` 연도 collision(월키만 사용) → `YYYY-MM` 키 / `computeHourlyDistribution` 이달·취소 필터 / `computePopularTreatments` designCategory 우선 / `monthReturnRate` prefix 기준 / 단계 가드 requiredStep / repair 라벨 / 랩핑 시술시간 |

### i18n
| # | 심각도 | 내용 |
|---|--------|------|
| I1 | HIGH | `ConsultationLocaleButton` 이 `setLocale` 사용 → 상담 후 언어 복원 깨짐. `setConsultationLocale` 로 수정 |
| I2 | HIGH | 사전상담 다국어 페이지 하드코딩 한국어(날짜/슬롯/에러/디자인 힌트) → 4개 언어 키 추가 + locale-aware 포맷 |
| I3 | MEDIUM | `treatment-sheet` 가격이 ₩ 고정 → `formatLocaleCurrency` / zh `channel.kakao` 한국어 → `KakaoTalk` / DiscountModal·step1 aria 하드코딩 → t() |

### 빌드 / 안정성
- `consultation/page.tsx`·`treatment-sheet/page.tsx` `useSearchParams` Suspense 누락 → inner+Suspense 래핑(프로덕션 빌드 안정)
- 온보딩 단계 게이팅(pricing/surcharges/notice) + 완료자 재진입 차단 + `setShopSettings` await + complete updateShop 실패 처리
- 포트폴리오 `is_staff_pick/is_popular` DB 영속화(컬럼 추가) / `togglePhotoVisibility` 레이스 / `isPortfolioPhoto` shopId 가드 / ShareCard imageDataUrl falsy 처리 / PortfolioOverlay designScope 매핑 / 카테고리 SSOT magnet 복원

### UI/UX (ui-ux-pro-max)
- 기준선 우수 확인: Button(focus ring·min-h-44·loading), Input(label+htmlFor·16px·에러 하단), 전화 input `tel`/`inputMode`, 네비 SVG 아이콘, SlotPicker 터치타겟(칩 72px·슬롯 44px·gap-2)
- 개선 적용: SlotPicker 선택형 칩/슬롯 `aria-pressed` + 날짜칩 `aria-label`(다국어 손님 스크린리더 상태 안내)

## 알려진 잔여 (양성 / 후속 권장)
- **ConsultationLayout React key 경고** — dev 전용. 콜스택 20프레임 전부 프레임워크(node_modules), 사용자 코드 프레임 0. Next.js App Router 가 레이아웃 `{children}` 을 키 없는 배열로 전달해 발생. **프로덕션 빌드에서 제거됨**(빌드 통과 확인). 래퍼로 우회 시 hydration mismatch 발생하여 원복 — user-land 수정 부적절.
- 공개 버킷 3개 listing 허용(WARN) — Storage RLS 정책으로 shopId prefix 제한 후속 권장
- Auth 유출 비밀번호 보호 비활성 — Supabase 대시보드 토글
- 남은 anon SECURITY DEFINER RPC들(get_consultation_link_public 등)은 공개 사전상담 플로우용 의도된 설계(내부 검증 보유)
- `share_card_public_select` RLS 가 share_card 보유 전체 레코드 노출 — 단건 RPC 전환 후속 권장(현재 공유 의도 데이터)

## 검증 로그
- `npx tsc --noEmit` → 0
- `pnpm lint` → 에러 0 (img/exhaustive-deps 경고만)
- `pnpm build` → exit 0, 전 라우트 컴파일
- Chrome 실측: 슬롯 off-by-one 수정 확인, 전 주요 라우트 콘솔 에러 없음
- Supabase advisor: 위험 anon RPC 3종 + 레거시 오버로드 제거 확인
