# BDX 기능 상세 명세 (Feature Detail)

> 각 기능의 개요, 인터뷰 근거, 기술 설계, 히스토리를 기록
> 최종 업데이트: 2026-03-15

---

## 1. 제품 방향 전환

### 개요
BDX를 "상담 시스템"에서 **"고객 관리 CRM + 시술 기록 시스템"**으로 전환한다.

### 인터뷰 근거
- 고객 선호 데이터를 체계적으로 관리하지 못함 (기억/메모 의존)
- 시술 중 고객 특징을 계속 확인하는 피로도
- 과거 시술 디자인 사진 재검색 빈번
- 가격 오해로 인한 트러블 발생
- 기존 ERP(공비서 등)는 예약/매출 관리 중심 → 고객 성향 관리 부재

### 핵심 기능 3가지
1. **스케줄 관리 혁신** — 각 고객별 시술 성향 관리
2. **시술 사진 포트폴리오** — 과거 시술 디자인 빠른 검색
3. **고객 성향 CRM** — 태그 기반 고객 특성 데이터화

### 차별화 포인트
| 기존 ERP | BDX |
|----------|-----|
| 예약 관리 | 고객 성향 관리 |
| 매출 관리 | 시술 사진 데이터 |
| 텍스트 메모 | 태그/색상 직관적 표시 |
| 한국어만 | 다국어 상담 (ko/en/zh/ja) |

### 히스토리
| 날짜 | 내용 |
|------|------|
| 2026-03-09 | 클라이언트 피드백 기반 방향 전환 결정 |

---

## 2. 홈 화면 / 스케줄 관리

### 개요
홈 화면을 스케줄표 중심으로 재구성. 각 예약 카드에 고객 이름, 시술 종류, 고객 특징 태그를 한눈에 표시한다.

### 상세 요구사항
- 스케줄표가 홈 화면에서 가장 크게 표시
- 예약 카드 구성:
  ```
  김지영
  자석젤
  [큐티클 민감] [오버레이 선호] [유지력 약함]
  ```
- 태그는 색상 코딩 (2.3 참조)
- 디자인 확정(🟢) / 현장 상담 필요(🟡) 아이콘 표시

### 기술 고려사항
- 기존 `(main)/home/` 페이지 리팩토링
- 고객 태그 데이터 모델 추가 (Zustand store 또는 data)
- 예약 카드 컴포넌트 신규 생성

### 히스토리
| 날짜 | 내용 |
|------|------|
| 2026-03-09 | 요구사항 정리 |
| 2026-03-10 | 홈 예약 카드가 `customerId` 기반으로 고객 주요 태그를 칩 형태로 표시하도록 반영. pinned 태그가 없을 때도 기존 고객 태그를 우선순위대로 노출 |
| 2026-03-10 | 고객 태그 색상 규칙을 공통 resolver로 통합. `accent` 커스텀 색상 + 카테고리 기본 색상을 스케줄, 고객 상세, 예약 연결 모달, 시술 전 알림, 기록 상세에 일관 적용 |
| 2026-03-10 | 예약별 사전 상담 완료 시점을 저장하고, 홈/예약 리스트/캘린더/예약 상세에 `디자인 확정` / `현장 상담 필요` 준비 상태 아이콘을 추가 |
| 2026-03-10 | 기록 관리 페이지에서 `useShopStore` method selector가 새 배열을 반환하던 문제를 제거하고, 활성 디자이너 목록을 `designers` 기반 `useMemo` 파생값으로 바꿔 `getSnapshot` 무한 루프 경고를 수정 |
| 2026-03-12 | 홈/대시보드/기록/캘린더의 `today` 계산과 날짜 표기를 `Asia/Seoul` 기준으로 통일하고, 당일 예약/상담 집계도 한국 시간 기준으로 비교되도록 수정 |
| 2026-03-12 | KST 공통 날짜 유틸을 확장해 예약/상담/고객/포트폴리오 저장 타임스탬프, mock 상대 날짜, 포트폴리오 기본 날짜, 상대 날짜 표시까지 한국 시간 기준으로 통일 |
| 2026-03-12 | 홈 `오늘의 예약` 카드의 `고객 연결 필요` 버튼에 `self-start text-left` 정렬을 적용해, 고객 정보 컬럼 안에서 가운데 정렬처럼 보이던 CTA를 왼쪽 시작선에 맞춰 정리 |
| 2026-03-12 | `records` 일간 디자이너 캘린더의 드래그 예약 카드도 홈 예약 카드처럼 서비스 라벨과 고객 태그를 카드 내부에 보여주도록 정리하고, 시간 슬롯 높이를 키워 태그 줄이 자연스럽게 보이게 조정 |
| 2026-03-12 | 홈 `오늘의 예약` 카드에 예약 참고 이미지와 고객 포트폴리오 사진을 작은 썸네일 스트립으로 추가하고, 예약 폼 업로드는 `blob:` 임시 URL 대신 data URL로 저장하도록 바꿔 새로고침 뒤에도 썸네일이 깨지지 않게 조정 |
| 2026-03-15 | 홈 `오늘의 예약` 카드에서 포트폴리오/참고 썸네일과 CTA가 같은 줄에 몰리며 모바일 텍스트 영역이 무너지던 문제를 수정하고, 모바일에서는 `시간 + 본문`과 `썸네일 + 액션`을 두 줄로 분리해 고객명/태그가 세로로 찌그러지지 않도록 정리 |
| 2026-03-15 | 홈 메인 카드 섹션 제목을 `오늘의 예약`에서 `오늘의 상담`으로 바꿔, 실제 카드 액션과 운영 용어가 더 자연스럽게 맞물리도록 조정 |
| 2026-03-15 | 홈 통계 카드의 예약 지표 라벨을 `상담 예정`으로 조정하고, 온보딩 가이드/인트로 데모에 남아 있던 `오늘의 예약` 문구도 `오늘의 상담` 기준으로 정리해 홈 진입 카피를 일관되게 통일 |
| 2026-03-15 | 홈 Hero CTA의 보조 액션 제목도 `새 예약 등록`에서 `새 상담 등록`으로 바꿔, 홈 첫 진입 액션 카피를 상담 중심 흐름에 맞게 정리 |

---

## 3. 포트폴리오 기능

### 개요
시술 사진을 그리드 형태로 관리하는 포트폴리오 페이지. 사진 클릭 시 시술 날짜, 고객 정보, 상담 기록, 시술 내용, 최종 가격을 확인할 수 있다.

### 사용 시나리오
1. 고객이 "예전에 다른 손님이 했던 네일 스타일" 요청
2. 포트폴리오에서 사진 검색
3. 당시 시술 구성과 가격 확인
4. 가격 오해 없이 시술 진행

### 상세 요구사항
- 사진 그리드 레이아웃 (Pinterest 스타일)
- 사진 상세 정보:
  - 시술 날짜
  - 고객 정보 (연결)
  - 상담 기록 (연결)
  - 시술 내용 (종류, 컬러, 파츠 등)
  - 최종 가격
- 검색/필터: 시술 종류, 기간, 고객, 가격대

### 기술 고려사항
- 기존 포트폴리오 사진 업로드 페이지 확장
- 이미지 저장: Vercel Blob 또는 외부 스토리지
- 포트폴리오 ↔ 고객카드 ↔ 상담기록 관계 설계

### 히스토리
| 날짜 | 내용 |
|------|------|
| 2026-03-09 | 요구사항 정리 |
| 2026-03-10 | 포트폴리오 갤러리에 시술 종류/기간/가격 필터를 추가하고, 검색 대상을 태그·컬러·디자인 타입까지 확장. 업로드 폼도 메타데이터 입력을 지원하도록 보강 |
| 2026-03-10 | 대시보드 `인기 시술 Top 3`를 포트폴리오 `시술` 사진 기반 썸네일 카드로 교체하고, 클릭 시 컬러번호와 가격을 확인할 수 있는 팝업을 추가 |
| 2026-03-10 | 포트폴리오 사진을 localStorage base64 저장 대신 Supabase Storage `portfolio-images` 버킷 + `portfolio_photos` 메타데이터 테이블로 전환하고, 앱 시작 시 DB hydrate 되도록 연결 |
| 2026-03-10 | 기존 localStorage 포트폴리오 사진을 첫 로드 시 Supabase로 자동 이관하고, 포트폴리오 업로드/삭제/초기화 결과를 토스트/상태 메시지로 확인할 수 있게 보강 |
| 2026-03-17 | 포트폴리오 업로드의 고객 검색 드롭다운에 별도 `showCustomerDropdown` 상태와 외부 클릭 닫힘 처리를 추가해, 고객 선택/직접 추가 후에도 결과 목록이 남아 있던 모바일 UX 문제를 수정 |
| 2026-03-17 | 포트폴리오 업로드의 `시술 종류` 입력을 자유 텍스트 대신 선택형으로 정리해, `원컬러/단색+포인트/풀아트/이달의 아트` 메타데이터가 더 일관되게 저장되도록 보강 |

---

## 4. 고객 카드

### 개요
고객별 성향 정보를 태그로 관리. 강조, 색상, 상단 고정 기능 지원.

### 태그 예시
| 태그 | 설명 |
|------|------|
| 큐티클 민감 | 큐티클 관리 시 주의 필요 |
| 오버레이 선호 | 오버레이 적용 선호 |
| 두께 얇게 | 얇은 시술 선호 |
| 유지력 약함 | 자주 떨어짐, 보강 필요 |

### 고객 카드 기능
- 태그 추가/삭제
- 태그별 강조 표시 (✔)
- 태그별 색상 지정
- 중요 태그 상단 고정
- VIP 비주얼 카드: 마지막 시술 사진 미리보기
- 시술 이력 타임라인

### 기술 고려사항
- 기존 `(main)/customers/` 페이지 확장
- 고객 타입 정의에 태그 필드 추가
- 태그 시스템 공용 컴포넌트화 (스케줄 카드에서도 사용)

### 히스토리
| 날짜 | 내용 |
|------|------|
| 2026-03-09 | 요구사항 정리 |
| 2026-03-10 | 시술 전 알림 모달을 고객 주의사항 확인 단계로 고도화하고, 태그 값 기반 세이프티 레벨(높음/중간/참고/선호)과 안내 문구를 예약 상세/알림 모달에 적용 |
| 2026-03-10 | 고객 상세의 태그 편집 UI에 `강조` 토글, 색상 선택, `상단 고정` 토글, 고정 태그 드래그 정렬을 추가하고 저장 전 로컬 미리보기를 지원 |
| 2026-03-15 | 고객 상세 시술 사진 그리드의 삭제 버튼이 hover에서만 나타나던 구조를 수정해, 터치 디바이스에서는 항상 노출되고 데스크톱에서만 hover reveal을 적용하도록 반응형 접근성을 보강 |
| 2026-03-15 | 고객 상세 최근 시술/시술 사진 갤러리에서 storage 기반 항목의 빈 `imageDataUrl`이 `next/image`에 그대로 전달되던 문제를 막기 위해, 렌더 가능한 src가 있는 시술 사진만 갤러리/팝업 데이터로 사용하도록 필터링 |
| 2026-03-16 | 고객 상세 `시술 성향 태그` 편집의 모바일 헤더/고정 태그 안내 영역에서 버튼과 배지가 설명 텍스트 폭을 과하게 잠식하던 문제를 수정하고, 모바일에서는 액션을 아래로 내리고 `whitespace-nowrap` 처리해 설명 문장이 세로처럼 부서지지 않도록 보강 |

---

## 5. 시술 전 알림

### 개요
예약 시간에 맞춰 고객의 주요 특이사항을 알림 형태로 표시. 시술 실수 방지 목적.

### 알림 예시
- "이 고객은 큐티클이 민감합니다"
- "이 고객은 오버레이를 선호합니다"

### 세이프티 태그 색상 코드
| 색상 | 의미 |
|------|------|
| 🔴 빨강 | 큐티클 예민 |
| 🟠 주황 | 유지력 낮음 |
| 🟡 노랑 | 특별 선호사항 |
| 🟢 초록 | 이상 없음 |

### 기술 고려사항
- 예약 시간 기반 트리거 (현재는 서버 없으므로 화면 진입 시 표시)
- 알림 모달/토스트 컴포넌트
- 고객 태그 데이터 연동

### 히스토리
| 날짜 | 내용 |
|------|------|
| 2026-03-09 | 요구사항 정리 |
| 2026-03-12 | staff용 상담 요약의 최종 결제 카드에서 raw i18n key `consultation.basePrice`가 그대로 보이던 문제를 수정하고, 실제 `breakdown.subtotal` 값에 맞춰 `summary.subtotal` 라벨을 사용하도록 정리 |

---

## 6. 상담 UX 간소화

### 개요
기존 상담 플로우(고객정보 → 시술옵션 → 캔버스 → 저장)를 6단계로 재구성. 최대한 간단하게.

### 신규 상담 플로우
| Step | 내용 | 선택지 |
|------|------|--------|
| 1 | 시술 부위 | 손 / 발 |
| 2 | 시술 종류 | 원컬러 / 그라데이션 / 자석젤 / 아트 |
| 3 | 기본 조건 | 제거 여부 / 연장 여부 |
| 4 | 참고 이미지 | 업로드 |
| 5 | 고객 특이사항 | 큐티클 민감 / 오버레이 선호 / 유지력 약함 등 |
| 6 | 상담 저장 | 확인 + 저장 |

### 기존 → 신규 매핑
| 기존 | 신규 |
|------|------|
| consultation/customer/ | Step 4: 고객 정보 입력 (특성 선택 다음) |
| consultation/step1/ | Step 1-2: 부위 + 종류 |
| consultation/step2/ | Step 3: 기본 조건 |
| consultation/step3/ | Step 5: 특이사항 |
| consultation/canvas/ | 옵션 기능으로 분리 (9장 참조) |
| consultation/summary/ | Step 6: 저장 |

### 기술 고려사항
- 기존 `consultation/` 디렉토리 구조 변경
- Step 4 참고 이미지: 카메라/갤러리 업로드
- Step 5 특이사항: 고객 카드 태그 시스템과 연동
- i18n 키 전체 재구성

### 히스토리
| 날짜 | 내용 |
|------|------|
| 2026-03-09 | 요구사항 정리 |
| 2026-03-12 | `/consultation` 시작 화면의 히어로 헤더는 BDX 세로 로고 중심으로 정리하고, 별도 심볼 배지는 제거해 더 단정한 브랜드 랜딩으로 조정 |
| 2026-03-12 | 메인 상담 흐름을 `step1 → step2 → 특성 선택 → customer → summary` 순서로 재정렬하고, `/consultation/customer` 는 특성 선택 다음 단계에서 열리도록 이동. 시작 버튼/재개 라우팅/요약 뒤로가기도 새 순서에 맞게 함께 조정 |
| 2026-03-12 | 홈 `오늘의 예약`, 예약 캘린더, 고객 상세의 `상담 시작` 진입은 기존 `/consultation/customer` 직접 이동 대신 `currentStep=step1_basic` 상태로 초기화 후 `/consultation/step1` 로 보내도록 수정. 예약 메모와 예약 언어는 기존처럼 유지되도록 sessionStorage/locale 동기화도 함께 보강 |
| 2026-03-12 | 고객 정보 단계에서 기존 고객을 선택하거나 예약에 이미 연결된 고객이 있는 경우, `handleNext()`가 해당 `customerId`를 유지하도록 수정. 요약 저장에서 `consultation.customerId`가 비워져 새 고객이 중복 생성되던 문제를 방지 |
| 2026-03-12 | customer link 모드 상담 저장 성공 시 `/consultation/save-complete` 화면으로 남지 않고 바로 `/home`으로 이동하도록 바꿔, 저장 후 운영 홈 화면으로 자연스럽게 복귀되도록 조정 |

---

## 7. 가격 입력

### 개요
가격을 상담 단계가 아닌 **시술 종료 후** 마지막 단계에서 입력. 네일 시술 특성상 가격 변동이 많으므로 사후 확정 방식이 현실적.

### 가격 입력 구조
```
기본 가격: ₩ ____
+ 추가 옵션: ₩ ____  (파츠, 아트 등)
+ 추가 시술: ₩ ____
─────────────────
최종 가격: ₩ ____  → 확정
```

### 기술 고려사항
- 시술 완료 후 진입하는 별도 화면
- 상담 기록 + 고객 카드 + 포트폴리오에 가격 저장
- 가격 히스토리 (동일 고객 이전 가격 참조)

### 히스토리
| 날짜 | 내용 |
|------|------|
| 2026-03-09 | 요구사항 정리 |
| 2026-03-12 | 시술 확인서(`treatment-sheet`)에서 가격 확정 시 할인/예약금을 함께 적용하도록 보강. 정액 할인/할인율/예약금은 숫자 프리셋과 +/- 선택 UI로 조정하고, 확정 시 `consultation.discount`, `consultation.deposit`, `finalPrice`를 레코드에 함께 저장하도록 정리 |
| 2026-03-15 | 시술 확인서 `저장하기`에서 `reset()`이 먼저 실행돼 `useConsultationGuard()`가 `/consultation`으로 리다이렉트하던 문제를 수정하고, `/home` 이동을 먼저 수행한 뒤 상담 상태를 정리하도록 변경 |
| 2026-03-15 | 추가로 `treatment-sheet`는 유효한 `consultationId`로 record를 불러온 경우 consultation store의 `entryPoint`가 비어 있어도 guard를 건너뛰도록 조정해, record 기반 저장 완료 후 `/consultation`으로 역이동하던 race를 차단 |

---

## 8. 상담 입력 방식

### 개요
상담 입력을 두 가지 경로로 제공한다.

### 방식 1: 예약 후 상담 링크 발송
- 네이버 예약 / 카카오 채널 예약 확정 후
- 고객에게 상담 링크 전달
- 고객이 모바일에서 시술 요청사항 미리 입력

### 방식 2: 매장 태블릿 모드
- 매장 대기 고객이 태블릿으로 상담 입력
- 기존 태블릿 대응 강화

### 기술 고려사항
- 링크 발송: 고유 URL 생성 (세션 기반)
- 태블릿 모드: 전용 레이아웃 / 키오스크 모드
- 두 경로 모두 동일한 상담 데이터 구조로 저장

### 히스토리
| 날짜 | 내용 |
|------|------|
| 2026-03-09 | 요구사항 정리 |
| 2026-03-12 | 고객 상담 링크(`entry=customer-link`) 진입 시 `src/app/consultation/customer/page.tsx` 에서 2.5초 브랜드 스플래시를 먼저 보여준 뒤 상담 폼으로 전환되도록 보강. 샵명이 있으면 함께 노출하고, 같은 세션에서는 반복 진입 시 스플래시를 다시 띄우지 않도록 `sessionStorage` 키로 제어 |
| 2026-03-12 | 고객 상담 링크/QR 생성 URL을 `/consultation` 루트로 통일하고, 레거시 `/consultation/customer?entry=customer-link...` 접근도 query params를 유지한 채 루트 첫 화면으로 리다이렉트하도록 수정. 루트 페이지가 링크 prefills를 직접 hydrate하고 스플래시도 같은 query 기준으로 유지 |
| 2026-03-12 | `/consultation` 게이트웨이에서 customer-link/staff 플로우를 분리. customer-link는 스플래시 이후 store의 `entryPoint` 기준으로 step 라우팅하고, staff 전용 디자이너 선택 UI는 customer-link 모드에서 렌더링하지 않도록 수정 |
| 2026-03-12 | customer-link 루트 진입의 시작 단계를 `/consultation/customer`로 조정해 고객용 상담 폼으로 시작되게 수정. `/consultation/step1`으로 오동작하던 경로를 제거하고, 레거시 `/consultation/customer?entry=customer-link...` 접근은 query 유지 후 루트로 재정렬 |
| 2026-03-12 | customer-link 진입은 URL을 `/consultation`으로 유지하면서 고객용 화면을 표시하도록 미들웨어 rewrite(`/consultation` → 내부 `/consultation/customer`)를 적용. 주소창은 루트 경로를 유지하고 customer-link 파라미터도 보존 |
| 2026-03-12 | customer-link는 미들웨어 rewrite 없이 `/consultation` 페이지 자체에서 고객 정보 화면을 직접 렌더링하도록 재조정. customer 단계 이동(`traits`/`summary` back)도 `/consultation`로 통일해 고객용 폼이 샵 전용 화면으로 전환되지 않게 분리 |
| 2026-03-12 | 상담 단계 순서를 `고객 정보`를 Step 1로 앞당기도록 재정렬. staff 시작 버튼은 `/consultation/customer`부터 시작하고, customer 페이지 다음 단계를 `/consultation/step1`로 변경했으며 summary 뒤로가기는 staff=`traits`, customer-link=`/consultation`으로 분기 |
| 2026-03-12 | customer-link도 Step 1(고객 정보) 다음에 `/consultation/step1`로 이어지도록 수정해, `다음` 버튼이 곧바로 summary로 점프하던 흐름을 제거하고 staff/customer-link 모두 `고객 정보 → 기본 조건 → 시술 유형 → 고객 성향 → 요약` 순서를 따르도록 정리 |
| 2026-03-15 | customer-link 포트폴리오 선택은 동일 이미지를 다시 누르면 선택 해제되도록 토글 UX로 수정하고, `PortfolioBrowser`가 더 이상 placeholder 썸네일이 아닌 실제 `shopId` 기준 포트폴리오 이미지를 직접 읽어오도록 보강. 동시에 `BDX 포트폴리오 크게 보기` 전체화면 브라우저를 추가해 고객이 큰 사진을 감상하며 선택/취소할 수 있게 정리 |
| 2026-03-12 | 현장 상담 시작/저장 시 선택한 `designerId`를 연결된 예약에도 함께 저장하도록 보강해, `records` 탭 디자이너 데이 그리드에서 예약 카드가 선택한 디자이너 열에 표시되도록 정리 |
| 2026-03-12 | 홈 `새 예약 등록` 모달의 submit 경로에서도 `designerId`를 누락하지 않도록 수정해, 예약 생성 직후부터 `records` 디자이너 데이 그리드가 선택한 담당 선생님 열에 바로 표시되게 정리 |

---

## 9. 캔버스 옵션화

### 개요
손가락 캔버스 기능을 기본 상담 필수가 아닌 **옵션 기능**으로 전환.

### 변경 내용
- 기본 상담 6단계에서 캔버스 분리
- 설정에서 ON/OFF 토글
- 디테일 기록이 필요한 샵만 활성화

### 기술 고려사항
- 기존 `consultation/canvas/` 경로를 옵션 분기로 변경
- 설정 store에 캔버스 활성화 플래그 추가
- 캔버스 ON일 때만 상담 플로우에 추가 단계 삽입

### 히스토리
| 날짜 | 내용 |
|------|------|
| 2026-03-09 | 요구사항 정리 |

---

## 10. 시술 리포트 기념품화

### 개요
시술 완료 후 **디지털 카드** 형태의 시술 리포트를 생성. 외국인 관광객에게 "여행의 추억"으로, 내국인에게는 "시술 기록"으로 활용.

### 리포트 구성
1. **비주얼 다이어리**: 매거진 레이아웃 (시술 전 → 디자인 시안 → 시술 후)
2. **K-뷰티 인증 마크**: "Certified by BDX" + 샵 낙관
3. **퍼스널 케어 팁**: 피부톤 맞춤 컬러 추천, 관리 팁 (다국어)
4. **QR코드**: 리포트 접근용

### 공유/마케팅 기능
- SNS 공유 버튼 (인스타 스토리용 이미지 생성)
- 비포/애프터 숏폼 영상 자동 생성 (15초)
- 친구 추천 할인 코드 (Referral)
- 샵 계정 + 해시태그 자동 삽입

### 기술 고려사항
- HTML → 이미지 변환 (html2canvas 또는 서버사이드)
- QR 코드 생성 라이브러리
- 이미지/영상 생성: 클라이언트 vs 서버
- SNS 공유 API 연동

### 히스토리
| 날짜 | 내용 |
|------|------|
| 2026-03-09 | 요구사항 정리 |

---

## 11. 대시보드 개선

### 개요
매출 중심에서 **성과 중심 + 현장 중심**으로 대시보드 재구성.

### 신규 위젯/기능
| 위젯 | 설명 | 우선순위 |
|------|------|----------|
| 업셀링 리포트 | BDX 상담 후 추가된 옵션 매출 | P1 |
| 운영 효율 필터 | 확정/현장상담 아이콘 | P1 |
| 비주얼 퀵 메뉴 | 인기 메뉴 사진 썸네일 + 팝업 | P1 |
| 글로벌 대시보드 | 외국인 예약 수, 언어별 준비상태 | P1 |
| 재방문 골든타임 | 28일 주기 리마인더 대상 리스트 | P2 |
| 디자이너 지표 | 지정예약률, 상담완료율 차트 | P2 |

### 기술 고려사항
- 기존 `(main)/dashboard/` 페이지 리팩토링
- 데이터 집계 로직 (현재 목업 데이터 기반)
- 차트 라이브러리 선정 (현재 framer-motion 활용 가능)

### 구현 메모
- 업셀링 리포트는 `ConsultationRecord` 기준으로 디자인/표현기법/파츠/추가 컬러 surcharge를 집계해 전체 매출과 분리된 추가 옵션 매출로 노출
- 글로벌 대시보드는 `BookingRequest.language` 와 `preConsultationCompletedAt` 기반으로 오늘의 외국인 예약 수와 언어별 준비율을 계산
- 재방문 골든타임은 고객 `lastVisitDate + 28일` 규칙으로 이번 주 대상자를 선별하고, 고객별 리마인더 문구 복사 액션을 제공
- 테스트상점(`shop-1773300748626-x8a073`)은 live DB anon key로 `customers` insert가 RLS에 막히므로, `customer-store` hydrate 시 월간 재방문 더미 고객 3명을 merge해 대시보드/홈에서 골든타임 타깃이 항상 보이도록 구성
- 디자이너 지표는 현재 데이터 구조에 맞춰 `지정 예약률` 대신 `예약 배정률` 과 `상담 완료율` 을 노출해 과장 없는 운영 지표로 정리
- VIP 고객 TOP 5는 동명이인 고객이 있어도 React list key가 충돌하지 않도록 고객 `id` 를 함께 집계해 렌더링 key로 사용

### 히스토리
| 날짜 | 내용 |
|------|------|
| 2026-03-09 | 요구사항 정리 |
| 2026-03-10 | 업셀링 리포트, 글로벌 대시보드, 재방문 골든타임 알림, 디자이너 운영 지표를 실제 store/analytics 기반 집계로 구현 |
| 2026-03-10 | VIP 고객 TOP 5 집계에 고객 `id` 를 포함시켜 동명이인 데이터에서도 React key 충돌 없이 렌더링되도록 수정 |
| 2026-03-15 | `테스트상점` 확인용 더미 데이터 요청에 맞춰 실제 shop id(`shop-1773300748626-x8a073`) 전용 재방문 골든타임 seed 고객 3명을 `customer-store` hydrate 시 병합하도록 조정하고, live DB anon 권한에서는 `customers` insert가 RLS로 막힌다는 제약을 고려해 shop-scoped fallback으로 구현 |

---

## 12. 디자인 트렌드 분석

### 개요
샵 내 인기 디자인 랭킹을 제공하여 포트폴리오와 마케팅을 강화한다.

### 기능
- "이번 달 가장 인기 있는 상담 디자인" 선정
- 랭킹 등재 시 고객 알림
- 트렌드 기반 시술 추천

### 기술 고려사항
- 상담/시술 데이터 기반 집계
- 알림 시스템 연동
- 장기적으로 ML 기반 트렌드 예측 가능

### 히스토리
| 날짜 | 내용 |
|------|------|
| 2026-03-09 | 요구사항 정리 |

---

## 13. 인증 시스템 (구글 로그인)

### 개요
현재 BDX는 인증이 전혀 없는 프로토타입 상태. 실서비스를 위해 구글 OAuth 기반의 실제 인증 시스템을 구축한다.

### 현재 상태
- `src/app/(auth)/login/page.tsx` — Supabase Auth 이메일/비밀번호 로그인 후, 현재 샵의 온보딩 완료 여부에 따라 `/home` 또는 `/onboarding`으로 이동
- `src/app/(auth)/signup/page.tsx` — Supabase Auth 회원가입 후 빈 `shops` row와 owner `designers` row를 생성하고 즉시 로그인한 뒤 온보딩으로 연결
- `src/store/auth-store.ts` — Supabase `getSession()`과 `onAuthStateChange()`로 세션을 복원하고, `owner_id = auth.user.id` 기준으로 `currentShopId`와 원장 프로필을 복구함. 디자이너 PIN 비밀번호만 로컬에 보조 저장
- `src/app/auth/callback/route.ts` + `src/middleware.ts` — Supabase SSR 기반 Google OAuth code exchange와 세션 refresh를 처리함
- `src/app/(auth)/signup/google/page.tsx` — Google 계정으로 로그인했지만 아직 샵이 연결되지 않은 경우, 이름/샵 이름을 입력받아 바로 샵 계정으로 전환함
- `src/lib/demo-account.ts` — 고정 데모 계정 credential을 정의하고, auth CTA에서 해당 계정으로 즉시 로그인 가능
- Supabase에는 `demo@bdx.kr` / `shop-demo` 데모 샵이 seeded 되어 있어 로그인 없이 체험하기로 바로 실제 예시 데이터 화면을 볼 수 있음
- `src/lib/supabase.ts` — 공개 env가 누락된 배포에서는 placeholder 호출을 숨기지 않고, auth flow가 즉시 설정 오류 메시지를 보여주도록 guard 정보(`hasSupabaseEnv`)를 함께 제공
- 기존 `src/app/(auth)/lock/page.tsx` 잠금 화면은 제거되었고, 루트/가드/로그아웃 진입점은 모두 `/login`으로 통일됨
- 로그인 UX는 원장/직원 선택 대신 샵 계정 단일 진입으로 정리되었고, 디자이너는 로그인 후 앱 내부 프로필 개념으로 다뤄짐
- `src/middleware.ts` — 보안 헤더만 추가, 아직 Supabase 세션 검증은 하지 않음
- `src/app/(main)/settings/page.tsx` — 실제 Supabase Auth 비밀번호 재설정이 없는 상태에서 오해를 주던 로컬 비밀번호 변경 카드를 제거해, 설정 화면에는 현재 동작하는 인증 관련 액션만 남김
- `src/app/(main)/settings/page.tsx` — `계정 전환`과 `로그아웃`이 모두 같은 logout 흐름을 타고 있어 중복이던 상단 버튼을 제거하고, 인증 액션을 하나로 정리
- `src/components/layout/SideNav.tsx` — 샵 이름 옆에 붙던 역할 배지를 제거해, 사이드바 하단 정보를 매장명 중심으로 단순화
- `src/app/(main)/settings/page.tsx`, `src/store/shop-store.ts`, `src/lib/db.ts` — 설정의 `선생님 관리` 섹션에 디자이너 프로필 CRUD를 연결하고, 기존 고객/예약/상담/메모에 참조가 남아 있는 디자이너는 삭제되지 않도록 안전 가드를 추가
- `src/app/(main)/settings/page.tsx`, `src/store/shop-store.ts`, `src/lib/db.ts`, `src/components/ui/ProfileAvatar.tsx` — 디자이너 프로필 이미지를 Supabase Storage `designer-profile-images` 버킷에 업로드/삭제하고, DB에 저장된 경로를 앱 전역 아바타 렌더링의 단일 소스로 사용하도록 정리

### 기술 설계
1. **Supabase Auth 이메일/비밀번호 로그인**
   - 클라이언트에서 `signUp`, `signInWithPassword`, `getSession`, `onAuthStateChange` 사용
2. **Google OAuth 로그인/회원가입**
   - 클라이언트에서 `signInWithOAuth({ provider: 'google' })`
   - `/auth/callback` route에서 `exchangeCodeForSession()` 수행
   - middleware에서 `getUser()`로 세션 refresh
3. **샵 계정 ↔ 도메인 데이터 연결**
   - 회원가입 시 auth user id를 `shops.owner_id` 와 owner `designers.id` 에 연결
4. **샵 세션 복원**
   - 로그인/새로고침 시 `owner_id = auth.user.id` 로 샵을 조회하고 `currentShopId` 컨텍스트 복원
5. **추가 예정**
   - Supabase Dashboard provider credentials 연결, 비밀번호 재설정, 디자이너 개별 계정

### 히스토리
| 날짜 | 내용 |
|------|------|
| 2026-03-09 | 인프라 항목으로 추가 |
| 2026-03-10 | `/lock` 잠금 화면 라우트를 제거하고, 비로그인 진입/가드/로그아웃 경로를 `/login`으로 통일 |
| 2026-03-10 | `/login`을 샵 계정 단일 로그인 UX로 단순화하고, 데모 흐름에서 원장/직원 선택 없는 진입 방식으로 정리 |
| 2026-03-10 | `/signup` 회원가입 페이지를 추가하고, 회원가입 → 자동 로그인 → 미온보딩 계정 온보딩 진입 흐름을 로컬 샵 계정 기준으로 연결 |
| 2026-03-10 | 회원가입 시 빈 샵과 owner 디자이너를 실제 Supabase에 생성하고, 이후 로그인 세션과 core CRUD를 `currentShopId` 기준으로 분리해 샵 간 데이터 공유를 막음 |
| 2026-03-10 | 설정 `앱` 탭의 로컬 비밀번호 변경 카드를 제거해 실제 인증 상태와 맞지 않는 가짜 비밀번호 변경 UI를 노출하지 않도록 정리 |
| 2026-03-10 | 설정 `앱` 탭에서 같은 logout 동작을 하던 중복 `계정 전환` 버튼을 제거해 인증 액션을 단일화 |
| 2026-03-10 | 좌측 사이드바 하단의 역할 배지를 제거해 매장명 영역을 더 간결하게 정리 |
| 2026-03-10 | 설정 `선생님 관리` 섹션에 디자이너 프로필 CRUD를 추가하고, 연결 이력이 남은 디자이너 삭제는 차단하도록 안전 가드를 적용 |
| 2026-03-10 | 디자이너 프로필 이미지를 Supabase Storage `designer-profile-images` 버킷에 저장하고, 설정 업로드/삭제가 앱 전체 아바타에 즉시 반영되도록 전환 |
| 2026-03-10 | 로그인/회원가입의 로컬 계정을 Supabase Auth로 교체하고, 세션 복원 시 auth user와 연결된 샵/owner 프로필을 자동 hydrate 하도록 전환 |
| 2026-03-10 | `fetchShopByOwnerId()`를 `maybeSingle()` 기반으로 바꿔, 샵이 아직 연결되지 않은 세션의 초기화 과정에서 불필요한 콘솔 에러가 발생하지 않게 수정 |
| 2026-03-10 | `로그인 없이 서비스 체험하기`가 Supabase 데모 계정으로 바로 로그인하도록 바꾸고, `shop-demo`에 샘플 고객/예약/기록/포트폴리오 데이터를 seeded 해 실제 서비스처럼 체험 가능하게 구성 |
| 2026-03-10 | 배포 환경에 Supabase 공개 env가 누락됐을 때 auth 요청이 `placeholder.supabase.co`로 나가지 않도록 guard를 추가하고, 명시적인 배포 설정 오류 메시지를 반환하도록 수정 |
| 2026-03-10 | Google OAuth 버튼, callback route, Supabase SSR middleware를 추가해 Google 로그인/회원가입 진입 경로를 구현 |
| 2026-03-10 | Google 로그인 시 아직 샵이 없는 계정은 추가 입력 화면으로 보내 이름/샵 이름을 받은 뒤 즉시 샵 생성 + 온보딩을 진행하도록 통합 |
| 2026-03-15 | 설정 > 서비스의 기본 가격표에 `리페어 개당`, `연장 추가금` 편집 필드를 추가하고, 저장값을 `shopSettings.surcharges`와 상담 `연장/리페어` 카드 가격/실제 계산 로직에 함께 연결해 서비스 설정에서 바로 수정 가능하게 보강 |
| 2026-03-13 | 앱 공통 사이트 URL 상수를 `https://beauty-decision.com`으로 도입하고, 루트 메타데이터의 `metadataBase`/canonical/Open Graph URL 및 intro-demo 공개 링크를 새 도메인 기준으로 통일 |
| 2026-03-14 | 루트 메타데이터의 Open Graph / Twitter 공유 이미지를 `bdx-og-image-final.png`로 교체해, 새 브랜드 비주얼이 링크 미리보기에도 반영되도록 정리 |
| 2026-03-13 | 이메일 회원가입과 Google 회원가입 마무리 화면에 필수 동의 블록을 추가하고, 이용약관/개인정보 수집 및 이용 체크 상태를 sessionStorage로 이어받아 두 흐름 모두 동의 없이는 회원가입이 완료되지 않도록 정리 |
| 2026-03-13 | `/terms`, `/privacy` 전문 페이지와 공통 법률 문서 레이아웃을 추가하고, 한국 웹서비스 기준에 맞춘 이용약관/개인정보 처리방침 초안을 회원가입 동의 UI에서 바로 열람할 수 있도록 연결 |
| 2026-03-13 | 미들웨어 공개 라우트에 `/terms`, `/privacy`를 추가해 비로그인 상태에서도 약관 전문 페이지가 로그인 화면으로 막히지 않고 바로 열리도록 수정 |
| 2026-03-13 | 이용약관을 Recatch 스타일의 한국 SaaS 조문형 문체에 맞춰 `제1조 목적`부터 `제19조 준거법 및 분쟁해결`까지 재구성하고, BDX에 아직 없는 seat 과금/환불 조항은 제외한 채 실제 서비스 범위에 맞는 데이터 책임·이용제한·해지 구조로 정리 |
| 2026-03-13 | 개인정보 처리방침도 Recatch 수준의 한국형 조문 구조로 재정리하고, BDX 실제 데이터 흐름에 맞춰 처리 목적·항목·보유기간·파기·위탁·국외 이전·권리 행사·쿠키·비적용 범위 조항을 구성하되 유료 구독·웨비나·광고 추적 문구는 제외 |
| 2026-03-14 | 회원가입 필수 동의 UI에서 카드형/다크 바 처리를 제거하고, 밝은 배경 위에 바로 놓이는 인라인 문장형 레이아웃으로 단순화해 `이용약관` / `개인정보 수집 및 이용` 링크만 은은하게 강조되도록 조정 |
| 2026-03-15 | 모바일 `StatusBar`에서 매장명 옆 역할 배지를 제거해, 로그인 상태에서도 상단 헤더가 더 간결하고 좁은 폭에서 흔들리지 않도록 정리 |

---

## 14. 데이터베이스

### 개요
모든 데이터가 localStorage에 저장되는 프로토타입 → 실제 DB 도입. API 라우트를 통한 서버-DB 통신 구조로 전환.

### 현재 상태
- DB 관련 패키지 없음, `.env` 없음
- 데이터 흐름: `mock-*.ts` → Zustand → localStorage
- 이미지: base64로 localStorage에 직접 저장
- `shops`, `customers`, `consultation_records`, `booking_requests`, `portfolio_photos`는 이제 `shop_id` 기반 분리 구조를 사용하며, 신규 회원가입은 빈 샵으로 시작함

### 기술 설계
1. **DB**: Supabase PostgreSQL (Auth + DB + Storage 통합)
2. **ORM**: Prisma (`prisma/schema.prisma`)
3. **주요 테이블**: users, shops, customers, consultations, reservations, portfolio_photos, treatments
4. **API 라우트**: `app/api/` RESTful CRUD
5. **데이터 조회**: Zustand → SWR/React Query + API fetch
6. **이미지**: Supabase Storage (base64 → URL 참조)

### 마이그레이션 전략
1. DB 스키마 + API 라우트 먼저 구현
2. mock 데이터를 seed 스크립트로 DB 이관
3. Zustand store를 점진적으로 API 연동 전환
4. 전환 완료 후 mock 파일 제거

### 히스토리
| 날짜 | 내용 |
|------|------|
| 2026-03-10 | DB Full Package: 스키마 보완(`booking_requests.pre_consultation_data/completed_at`, `consultation_records.language`, `customers.preferred_language`, `shops.settings` JSONB), RLS 보안 강화(`get_my_shop_id()` 헬퍼 + shop 격리 정책 8개 테이블 전체 교체, anon INSERT 허용), Shop settings DB 동기화(`setShopSettings()` → fire-and-forget DB write, `syncShopSettingsFromShop()` → DB settings 복원), 코드 정리(`mock-dashboard.ts` 삭제, `KPICards.tsx` 하드코딩→실시간 계산, `database.ts` 타입 재생성) |
| 2026-03-10 | `booking_requests`, `portfolio_photos`에 `shop_id`를 추가/백필하고, fetch/mutation/store hydration을 모두 현재 샵 기준으로 제한해 멀티샵 데이터 분리를 구현 |
| 2026-03-09 | 인프라 항목으로 추가 |

---

## 15. 기록 관리 / 상담 이력 UX

### 개요
기록 탭에서 예약 상태 의미와 기록 상세 진입 동선을 더 직관적으로 정리해, 원장님이 예약/상담 기록을 빠르게 이해하고 탐색할 수 있게 한다.

### 구현 메모
- 예약 화면 상단에 `상담 필요` / `상담 완료` 상태 범례를 추가해 준비 상태 색상 의미를 즉시 이해할 수 있게 함
- 상담 기록 검색 입력 아래에 실제 검색 기준(`이름 / 전화번호 / 서비스명`)을 노출해 QA와 사용자 이해를 일치시킴
- 기록 리스트/미리보기/상세 페이지의 CTA는 `기록 상세보기` 기준으로 통일하고, 고객 이동 CTA는 `고객 상세로 이동`으로 분리함

### 히스토리
| 날짜 | 내용 |
|------|------|
| 2026-03-11 | 기록 탭 예약 화면에 상태 범례를 추가하고, 상담 기록 검색 기준을 이름/전화번호/서비스명으로 명시함 |
| 2026-03-11 | 기록 리스트/미리보기/상세 페이지 CTA를 `기록 미리보기` / `기록 상세보기` / `고객 상세로 이동` 기준으로 정리해 기록 상세 흐름과 고객 상세 흐름을 분리함 |
| 2026-03-16 | 기록 탭 예약 화면 상단 문구를 어색한 `날짜 상태 범례` 대신 더 자연스러운 `상담 상태`로 정리해, 색상 점이 무엇을 설명하는지 간결하게 읽히도록 수정 |
| 2026-03-15 | 모바일 `records` 예약 상세 바텀시트를 `max-h` 고정 시트 + 내부 스크롤 영역으로 재구성해, 세이프티 태그/참고사항 카드가 많을 때도 하단 액션 버튼과 `삭제` CTA가 잘리지 않고 끝까지 스크롤되도록 수정 |
| 2026-03-15 | 홈 `사전 상담 내용`, 기록 `상담 미리보기`, 대시보드 KPI 바텀시트도 같은 `flex column + internal scroll + safe-area` 패턴으로 맞춰, 모바일에서 긴 콘텐츠가 잘리거나 하단으로 밀리지 않도록 공통 반응형 동작을 정리 |
| 2026-03-15 | `records` 주간 캘린더의 날짜 스트립이 Monday-start 주간 배열인데도 cell index(`0..6`)를 요일로 사용하던 문제를 고쳐, 각 날짜가 KST 기준 실제 요일 라벨 아래에 표시되도록 수정 |
| 2026-03-15 | 모바일 기록 리스트 카드(`ConsultationListItem`)는 상단 정보를 2단 구조로 재배치하고 `기록 미리보기` CTA를 아래로 내려, 고객명/담당자/가격이 덜 눌리게 정리했으며 표현기법 태그는 모바일에서 2개 + `+N`으로 축약해 과밀도를 줄임 |
| 2026-03-15 | 기록 화면의 메인 탭과 상단 요약 카피를 `상담 관리` / `이번 주 상담` / `오늘 남은 상담`으로 조정하고, intro demo 안내도 같은 용어로 맞춰 상담 중심 운영 맥락을 통일 |
| 2026-03-15 | 디자이너 데이 그리드의 `미지정` 열은 실제로 담당 선생님이 비어 있거나 매칭되지 않는 일정이 있을 때만 생성되도록 바꿔, 배정된 일정만 있는 날에도 불필요한 `미지정` 열이 항상 노출되던 문제를 수정 |
| 2026-03-17 | 기록 탭 디자이너 데이 그리드의 `미지정` 열을 다시 항상 노출하도록 조정해, 일정이 모두 배정된 날에도 열 구성이 흔들리지 않고 드래그/비교 기준이 고정되게 정리 |
| 2026-03-17 | 기록 탭 예약 상세 바텀시트에 `고객용 상담 링크` 액션을 추가하고, 링크 생성 UI를 같은 바텀시트 내부 전환으로 처리해 nested modal 없이 customer-link URL을 복사/열 수 있게 정리. URL에는 `name`/`phone`/`note`/`lang`/`bookingId`와 함께 `shopId`/`shopName`까지 포함 |

---

## 16. 설정 / 운영 UX

### 개요
설정 화면에서 운영 정보 저장 결과를 명확히 보여주고, 데모/프로필 관리 액션을 더 직관적으로 정리해 원장님 운영 흐름을 단순화한다.

### 구현 메모
- 운영 정보 저장은 `shop` 기본 정보와 `shop.settings` 저장 결과를 모두 확인한 뒤 성공/실패를 피드백으로 보여줌
- 저장 중에는 버튼을 비활성화하고 `저장 중...` 상태를 노출해 중복 저장을 방지함
- `사용 가이드 다시 보기`는 실제 동작이 홈의 투어 오버레이 재실행이므로 `데모 가이드 다시 보기`로 문구를 수정함
- 선생님 관리의 프로필 관련 액션은 아이콘-only 버튼 대신 텍스트 버튼으로 정리해 `사진 삭제`와 `프로필 삭제`를 명확히 구분함

### 히스토리
| 날짜 | 내용 |
|------|------|
| 2026-03-11 | 운영 정보 저장이 실제 DB 결과를 기다리도록 바꾸고, 저장 중 상태와 성공/실패 피드백 배너를 추가함 |
| 2026-03-11 | 설정 앱 탭의 가이드 액션을 `데모 가이드 다시 보기`로 재정의하고, 선생님 관리의 프로필 이미지/프로필 삭제 액션을 텍스트 버튼으로 정리함 |
| 2026-03-17 | 설정 서비스 탭에 `시술 항목 관리` 토글 카드를 추가해 오프/그라데이션/프렌치/마그네틱/파츠/리페어/연장/오버레이 노출 여부를 바로 제어할 수 있게 정리 |

---

## 17. 개발 도구 / Supabase MCP

### 개요
개발 워크스페이스에서 Supabase MCP를 기본 조회 전용으로 연결해, 실수로 쓰기 작업을 실행할 위험을 낮춘다.

### 현재 상태
- 루트 `.mcp.json`에 Supabase MCP 원격 서버가 등록되어 있음
- 별도 `.env` 예시 파일은 없고, 첫 연결 시 클라이언트의 브라우저 인증 또는 수동 스코프 설정이 필요함

### 기술 설계
1. `.mcp.json`의 Supabase MCP URL에 `read_only=true`를 적용해 기본 모드를 조회 전용으로 제한
2. 최초 연결은 Supabase 공식 hosted MCP의 브라우저 인증 플로우를 사용
3. 특정 프로젝트로 범위를 제한해야 하면 `project_ref` 쿼리 파라미터를 추가하는 방식으로 확장

### 운영 메모
- 기본 URL: `https://mcp.supabase.com/mcp?read_only=true`
- 특정 프로젝트만 연결하려면 `https://mcp.supabase.com/mcp?project_ref=<PROJECT_REF>&read_only=true` 형태로 스코프 가능
- PAT 없이 브라우저 인증이 가능한 클라이언트에서는 추가 토큰 없이 로그인만 수행하면 됨

### 히스토리
| 날짜 | 내용 |
|------|------|
| 2026-03-10 | 워크스페이스 `.mcp.json`의 Supabase MCP를 `read_only` 기본값으로 조정하고, 최초 인증/프로젝트 스코프 운영 메모를 문서화 |
| 2026-03-17 | `src/lib/fonts.ts`에서 이미 `next/font/local`로 Pretendard를 로드하고 있는데도 `src/app/globals.css`에서 CDN `@import`를 중복 선언하던 부분을 제거해, dev 서버의 CSS 파싱 오류(`@import rules must precede all rules`)를 해소 |

---

## 18. QA 도구 / agent-browser 운영

### 개요
프로덕션 QA에서 `agent-browser` 세션이 서로 섞이지 않도록, 세션 네이밍/실행/종료 규칙과 공통 실행 진입점을 저장소 기준으로 정리한다.

### 현재 상태
- 브라우저 QA 문서는 존재하지만 `agent-browser` 전용 세션 관리 규칙은 없었음
- 동일 세션을 병렬 재사용하면 다른 페이지 상태가 덮여 QA 오탐이 발생할 수 있음

### 기술 설계
1. `scripts/agent-browser.mjs`에서 `open`, `status`, `close` 명령을 공통화한다.
2. 세션 이름은 `qa-<env>-<page>-<flow>-<timestamp>` 형식으로 생성해 목적이 드러나게 유지한다.
3. 프로덕션 주요 라우트는 `package.json`의 `pnpm qa:agent:*` 스크립트로 바로 실행 가능하게 연결한다.
4. 세션 산출물/상태 파일은 `.qa/`, `*.auth-state.json` 패턴으로 git에서 제외한다.

### 운영 메모
- 같은 `--session`은 병렬 호출하지 않고, 한 세션에서는 한 흐름만 순차 검증한다.
- 상태 재사용이 필요해도 `--session-name`, `--profile` 고정 운영보다 명시적 `state save/load`를 우선한다.
- 상세 운영 규칙과 예시는 `docs/qa-agent-browser.md`를 기준으로 한다.

### 히스토리
| 날짜 | 내용 |
|------|------|
| 2026-03-17 | `agent-browser` 세션 오염으로 인한 QA 오탐을 줄이기 위해 `docs/qa-agent-browser.md`, `scripts/agent-browser.mjs`, `pnpm qa:agent:*` 실행 스크립트를 추가하고 세션 네이밍/종료 규칙을 문서화 |
